[package]
name = "cantor-resident-service-benchmark"
version = "0.1.0"
edition = "2024"
publish = false

[workspace]

[lints.rust]
unsafe_code = "forbid"

[profile.release]
overflow-checks = true

[dependencies]
cantor_core = { path = "../../crates/cantor_core" }
cantor_service = { path = "../../crates/cantor_service" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
