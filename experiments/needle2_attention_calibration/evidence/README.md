# Calibration evidence

Each UUID directory is copied byte-for-byte from the isolated EVO-X2
calibration root after its remote `verify` command succeeds. The directory's
`manifest.json` is the strict local re-verification entry point.
