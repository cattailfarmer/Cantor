# Cantor operator bootstrap transaction P0

This experiment executes the initial-create transaction twice against one
fixed disposable public signed fixture, requires byte-identical secret-free
receipts and final no-listener diagnostics, destroys both random tokens and the
complete fixture root, and retains only a sanitized evidence report.

The independent verifier checks retained source, script, binary, dependency,
determinism, cleanup, and capability-denial evidence without invoking
`cantord`, creating a token, or mutating operator artifacts.

No production secret lifecycle, replacement, service, provider, installation,
delivery, effect, remote, FPGA, or Minecraft authority is claimed.
