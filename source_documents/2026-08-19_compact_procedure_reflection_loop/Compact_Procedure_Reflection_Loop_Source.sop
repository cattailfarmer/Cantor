Subject: Compact Procedure Reflection Loop Preserved Source

@ [source_uuid] c9e8cf7a-87e5-45cd-baa8-f729b9b08b6d
@ [captured_at_utc] 2026-08-19T16:39:12.3824607Z

& [UserSource]
  + "propose a small prototype of the basic function of loading a LLM and giving it a command by which it calls the tool in inference/attention window to execute some behavior and obtain results from that subroutine to reflect on those results in the final pass and then write to output"
  + "after we have a work flow set up, then we just grow it from there to meet requirements"

& [EstablishedPredecessors]
  + cantor-reflection-loop already proves an unmodified loopback llama.cpp model tool model sequence with a separate final reflection pass
  + cantor-compact-coordination-mcp now proves volatile exact context plus checkpoint custody behind a small compare-and-set handle
  + the missing seam is their integration rather than another generic host implementation

& [SelectedSlice]
  + pre-open one exact procedure context in a run-local compact session
  + offer the model one tool named advance_attention_procedure whose only argument is a bounded step quota
  + keep registry session sequence record and context bindings under host custody
  + validate the complete tool call before advancing
  + require the selected P0 advancement to reach terminal state
  + READ the exact terminal record and place the terminal result in the tool message for a later model pass
  + require the final pass to acknowledge exact session and outcome digests
  + preserve a sanitized report and invoke no external effect

& [Boundary]
  + use an ordinary loopback OpenAI-compatible chat-completions endpoint without modifying llama.cpp
  + do not claim hidden-state access live token insertion semantic truth persistence authentication or production readiness
  + do not contact EVO-X2 automatically and do not access OneDrive
  + deterministic fixtures may prove host semantics when no live model is selected
