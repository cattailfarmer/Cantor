Subject: Iterative Attention Procedure Loop P1 Slice 8A Bounded Checkpoint Handle Discovery Activation Source

@ [activation_source_uuid] caf74037-db1b-4dcb-be88-cfbf373ec678
@ [captured_at_utc] 2026-08-19T23:39:41Z
@ [governing_audit] C:\Project\Cantor\narrative\research\Cantor_Iterative_Attention_Procedure_Loop_P1_Provider_Free_Release_Audit_2026-08-19.sop

& [PreservedUserDirection]
  + "start another 5-hour turn"
  + the database should find SOP structures by given terms and correlation weights and return relationships records and proof
  + Cantor should resolve subjects use cases boundaries descriptions and applicable operations from compact attention inputs
  + the minimal primer should unfold further SOP when subject criteria are met

& [InheritedIntention]
  + Slice7C query requires a caller to already possess an exact handle and registry root
  + Slice7E correctly records handle discovery false and remains an immutable historical release candidate
  + the next separate primitive is bounded metadata discovery over a supplied in-memory registry without returning checkpoint bodies

& [SelectedImplementationClaim]
  + activate CheckpointHandleDiscoverySelector with optional expected registry root phase next operation transport position terminal flag and lowercase checkpoint digest prefix
  + all supplied selector constraints combine by logical AND
  + maximum results is required within one through twelve and deterministic digest-key order is preserved
  + missing expected registry root is an explicit bootstrap query and response marks caller_root_pinned false
  + supplied expected root must match exactly and response marks caller_root_pinned true
  + response returns exact DispatchCheckpointHandle values entry digests available match count returned count truncation state selector digest registry root and response digest
  + response embeds no checkpoints request bodies response bodies messages or transports
  + expose a bounded strict stdin-to-stdout discover-scripted-checkpoint-handles command

& [Acceptance]
  + unfiltered bootstrap discovers all twelve handles under bounded pagination semantics
  + root-pinned phase position terminal next-operation and digest-prefix selectors return exact deterministic intersections
  + zero-match results are valid and distinguishable from faults
  + wrong root invalid maximum invalid or uppercase short nonhex prefix response mutation count ordering duplicate body flag claim nonclaim and unknown fields fail closed
  + CLI rejects empty oversized trailing-content and extra-argument input
  + repeated discovery and serialization are byte-identical

& [IdentityBoundary]
  + bootstrap discovery identifies the current supplied registry snapshot but does not prove external provenance freshness or authorization
  + returned handles are not checkpoints and cannot resume without custody
  + selector matching is exact structural filtering and not semantic association search or correlation weighting
  + content digest is local integrity commitment and not authentication authorization or truth

& [Rollback]
  + P0 through Slice7E historical release root and all existing behavior remain unchanged
  + removing isolated handle_discovery module command exports and tests removes Slice8A without migration

& [Boundary]
  + no filesystem write database service process model HTTP socket stream timeout tokenizer clock randomness persistence concurrency remote EVO-X2 OneDrive hidden state KV cache live token effect FPGA or Minecraft activation
