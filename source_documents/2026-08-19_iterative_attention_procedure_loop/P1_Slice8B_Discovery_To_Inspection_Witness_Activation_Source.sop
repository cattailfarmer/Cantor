Subject: Iterative Attention Procedure Loop P1 Slice 8B Discovery To Inspection Witness Activation Source

@ [activation_source_uuid] e367536d-e7b8-4c74-b80a-3005db4d5c48
@ [captured_at_utc] 2026-08-19T23:56:40Z

& [PreservedUserDirection]
  + "start another 5-hour turn"
  + the lightest SOP primer should cause staged tool use and unfold more SOP only when subject criteria are met
  + prototype a flow where the model calls a tool obtains subroutine results reflects on them and writes output
  + a compact hash handle should select the exact authoritative material required for the next attention step

& [InheritedIntention]
  + Slice8A closes handle bootstrap while Slice7C exposes compact inspect but their staged composition is not yet one proven workflow
  + the minimal provider-free witness should begin without a known root or handle and end at a validated compact inspection

& [SelectedImplementationClaim]
  + activate ScriptedDiscoveryInspectionWitness over the canonical in-memory custody registry
  + bootstrap with no expected root maximum one and no filters
  + select the deterministic first returned handle
  + issue a second discovery selector pinned to the bootstrap root and exact full digest prefix maximum one
  + require pinned rediscovery to return the identical handle and entry digest
  + issue one exact inspect custody query using the pinned handle and root
  + require inspect response validation body exclusion and exact identity agreement
  + record all three request-response stages one workflow digest explicit zero provider/persistence/effect claims and fixed nonclaims
  + expose strict normalized checked artifact and witness-scripted-discovery-inspection CLI command

& [Acceptance]
  + bootstrap pinned discovery and compact inspect all validate independently
  + roots handles entry digests checkpoint phase next operation position and terminal flag agree across stages
  + full checkpoints requests responses messages and transports remain absent from the witness
  + repeated generation serialization CLI stdout and checked artifact are byte-identical
  + root handle stage order pinned state body claim provider claim workflow digest and unknown-field mutations refuse

& [IdentityBoundary]
  + witness proves deterministic provider-free protocol composition and not live model tool selection reflection or semantic understanding
  + selecting the first digest-ordered handle is a fixture policy and not relevance ranking
  + workflow digest is local integrity commitment and not authentication authorization provenance or truth

& [Rollback]
  + P0 through Slice8A and Slice7E historical release root remain unchanged
  + removing isolated discovery_inspection_witness module artifact command exports and tests removes Slice8B without migration

& [Boundary]
  + no filesystem write database service process model HTTP socket stream timeout tokenizer clock randomness persistence concurrency remote EVO-X2 OneDrive hidden state KV cache live token effect FPGA or Minecraft activation
