Subject: Derived CPMS I02C Canonical Document and Golden Activation Source

Description: Manager-derived activation question after the reviewed I02C formation. This source resolves the golden-generation lineage procedure before any code authority is granted.

@ [source_uuid] 424a618c-f441-4b41-b71e-3f3bc1dce03d
@ [source_kind] manager_derived_activation_question
@ [formation_commit] d8b2681

& [ActivationTarget] is the exact 24-requirement canonical profile E3B87260B96B5021EE18FF9E0CADAB54C08FABFFE3B17755804728C37467DD3A

& [ReuseBoundary] requires one normalization implementation
  + expose SchemaContractDirection and MachineSchemaRootKind tokens plus canonical_json_bytes as crate-private only
  + add document and bundle behavior in a new feature-gated schema_documents module
  + do not duplicate canonical JSON, mutate I02B public behavior, or add a dependency

& [GoldenLineageProblem] requires two checkpoints
  + generated bytes must bind the committed implementation source revision that produced them
  + checkpoint A adds pure code and structural tests, including an ignored explicit developer-only golden writer, then commits that passing capability
  + checkpoint B invokes the writer with checkpoint A's full lowercase commit revision, adds six generated fixtures, enables normal byte-equality verification, completes all gates, and commits the artifacts
  + the later containing commit is lineage for the generated files and is not substituted into their generation preimage

& [WriterBoundary] is development only
  + the public library returns a sorted in-memory artifact map and never accesses the filesystem
  + the ignored integration-test writer runs only with CANTOR_UPDATE_MACHINE_SCHEMA_GOLDENS=1 and an explicit repository-local destination
  + it refuses any destination outside the exact test golden directory and refuses an unclean or mismatched checkpoint-A source revision
  + normal tests never write and always compare checked-in bytes after checkpoint B

& [ExactSourceTouch] is closed
  + checkpoint A modifies crates/cantor_procedure_tool/src/lib.rs and schema_generation.rs and adds schema_documents.rs and tests/machine_schema_documents.rs
  + checkpoint B modifies only machine_schema_documents.rs and adds exactly six fixed JSON golden paths under tests/goldens/machine_schema
  + governance and metadata-only evidence manifests may accompany each checkpoint
  - never: Cargo, core, CLI, MCP, service, ecosystem, Windows, provider, model, runtime persistence, network, effect, production, or hardware source

& [ActivationQuestion] asks for exact hash lock, adversarial review, two-checkpoint plan, and phase signature before mutation
