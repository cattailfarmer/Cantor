Subject: Cantor Seeded Compiler Slice 4 External Inference Host Activation Source

@ [source_uuid] bfaa7604-0ee4-4fb7-acf5-8b02574d2af2
@ [conversation_uuid] 019f94c8-da23-7131-aef2-6ac02612d155
@ [captured_at_utc] 2026-08-20T17:10:00Z
@ [canonical_dependency] C:\Project\Cantor\specifications\Cantor_SOP_Seeded_Multi_Backend_Compiler_P0.sop
@ [Slice3_dependency] C:\Project\Cantor\proofs\Cantor_Seeded_Compiler_Slice3_Attention_Backend_Proof.sop

& [PreservedUserIntent]
  + [verbatim] "Cantor will do 3 different levels of compiling, compiling external binaries, compiling llama.cpp internal addons, and compiling attention patterns and procedures"
  + [interpretation] Slice4 activates the inference-host integration level through the preferred external adapter and does not authorize an internal llama.cpp addon

& [ExistingEvidence]
  + cantor-reflection-loop already demonstrates an unmodified loopback llama.cpp OpenAI-compatible two-pass tool cycle
  + first pass returns one structured route_attention call second stage invokes a pinned MCP tool and final pass reflects on the exact structured result
  + existing runner has finite timeout cancellation positive refusal control cases sanitized traces and offline report verification
  + the runner is experimental evidence and must not be retroactively classified as a signed admitted compiler artifact

& [Purpose]
  + compile one exact InferenceHostIntegration CandidateCompilationPlan and TypedSopIr into a deterministic external host integration candidate
  + bind host adapter provider protocol model tool lifecycle concurrency cancellation streaming compatibility evidence rollback and non-authority identities
  + reuse the proven external seam before considering internal llama.cpp modification

& [IntegrationCandidate]
  + binds seed plan IR source correspondence and unresolved account
  + binds adapter implementation identity source revision digest executable or package digest and configuration profile
  + binds provider family OpenAI-compatible chat-completions protocol loopback endpoint policy model selector and provider capability observations
  + binds exact model-visible tool name description input schema output schema annotations and schema digest
  + binds the MCP process identity executable digest configuration digest transport and tool result contract
  + binds initial request tool-call validation dispatch result reentry final-response validation and terminal state sequence
  + binds maximum provider passes maximum tool calls parallel-call policy timeout cancellation and response byte/token bounds
  + binds streaming mode as buffered_only in P0 because arbitrary semantic insertion during a running completion is not claimed
  + binds compatibility cases standard external seam evidence known limitations rollback and fixed non-authority

& [CompilationBoundary]
  + candidate projection is pure and permits semantic_read and source_read only
  + projection validates exact inference_host_integration backend profile seed IR plan purpose source correspondence and complete declared fields
  + projection may describe capabilities required by a future admitted runtime but does not acquire network process filesystem model or effect authority
  + no provider process MCP process or model call occurs during compilation

& [InternalAddonEscalation]
  + internal llama.cpp modification remains a distinct target identity rather than an optimization flag on the external candidate
  + internal target requires a separately signed measured limitation receipt naming one requirement the external seam cannot satisfy
  + receipt must bind standard-seam control measurement internal-extension comparison upstream revision ABI risk off-target tests and rollback
  + absence ambiguity or performance preference alone cannot satisfy the escalation gate

& [OutputContract]
  + deterministic strict InferenceHostIntegrationCandidate and projection digest bind all upstream and host-contract identities
  + candidate status remains proposed and cannot claim build verification admission installation registration execution or compatibility success
  + unknown fields substitutions partial contracts unsupported streaming hidden-state claims and internal-target substitution refuse

& [Acceptance]
  + equal complete inputs yield byte-identical candidate and digest
  + seed plan IR backend host revision endpoint model tool schema MCP digest flow limit cancellation streaming compatibility rollback and non-authority substitutions fail closed
  + existing reflection-loop source executable contract and tests remain unchanged
  + focused debug release workspace lint format evidence and diff gates pass before closure

& [Locked]
  + provider contact process launch network filesystem mutation model call procedure execution verification admission installation host registration internal llama.cpp modification KV cache hidden state logits native artifact FPGA OneDrive EVO-X2 and Minecraft
