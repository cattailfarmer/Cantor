Subject: Cantor Phase3 Topology Acquisition Lineage Serde Trait Correction Source

Description: Corrective source derived from operational fault 657fc4df-e100-4948-8042-7f4714eddc22. It resolves the impossible conjunction between deserializable carrier-bearing input claims and current output-only supplied digest carriers by separating strict deserializable metadata plans from owned carrier-bound invocation values constructed only by pure functions. This raw source is not canonical and grants no implementation or effect authority.

& [SourceIdentity] is exact
  + [source_snapshot_uuid] is 2e6aaaca-8265-43b4-b0d7-b793013c74fd
  + [conversation_uuid] is 01a02268-2614-7d80-9737-ea77f4aeacb1
  + [derived_from_fault_uuid] is 657fc4df-e100-4948-8042-7f4714eddc22
  + [invalidated_signature_uuid] is bdfc46ad-1d19-4a11-9051-d770cd96c9aa
  + [candidate_revision_profile] is cantor-phase3-topology-acquisition-lineage-forms/0.2
  + [status] is raw_preserved_unprocessed

& [PreservedSemantics] are mandatory
  + logical AcquisitionIdentity remains private nonzero u64 without generation uniqueness occurrence producer time proof or capability claim
  + AcquisitionScopeClaim remains a complete strict structural product
  + exactly two CompleteClaimed roles and one explicit FirstCompletionStrictlyPrecedesSecondStart claim remain
  + each accepted role must retain one complete current output-only ordered digest carrier and transitive assembly ancestry
  + current carrier validation digest rederivation and reconciliation rerun
  + Equal remains necessary and insufficient
  + successful evidence remains output-only ClaimOnly NonAtomicRepeatedInventoryEqual
  + producer admission issuance receipt conversion consumption physical truth and every effect remain absent

& [TraitClosureProblem] is exact
  + current WindowsSuppliedOrderedTopologyInventoryDigest is Serialize only with private fields
  + any value owning that carrier cannot derive Deserialize
  + previous profile required carrier-bearing input claim Deserialize and round trip
  + making the current carrier deserializable or replacing it with a digest locator or summary is forbidden

& [CorrectedTypeGraphCandidate] separates transport metadata from carrier ownership
  + AcquisitionIdentity remains strict Serialize and Deserialize metadata
  + AcquisitionScopeClaim remains strict Serialize and Deserialize metadata
  + CompletionDisposition and CausalOrderClaim remain strict closed Serialize and Deserialize metadata
  + AcquisitionLineageMetadataClaim owns identity scope and CompleteClaimed but no carrier and derives strict Serialize and Deserialize
  + OrderedAcquisitionPairMetadataClaim owns exact First and Second metadata plus causal relation and derives strict Serialize and Deserialize
  + RepeatedInventoryEvidencePlan owns exact profile metadata pair current reconciliation plan and requested grade and derives strict Serialize and Deserialize
  + AcquisitionLineageBinding owns one metadata claim and one complete current output-only ordered digest carrier and derives Serialize only
  + OrderedAcquisitionPairBinding owns exactly two lineage bindings and validated relation and derives Serialize only
  + RepeatedInventoryEvidenceClaim retains pair binding current reconciliation input and result grade and ClaimOnly and derives Serialize only

& [ConstructionFlowCandidate] is one pure path
  + caller strictly decodes and validates one bounded RepeatedInventoryEvidencePlan metadata value
  + caller separately supplies exactly two owned current output-only ordered digest carriers as function arguments
  + governed pure function binds First metadata to first carrier and Second metadata to second carrier
  + function validates both metadata scopes against carrier-exposed encoding limits root identity and current profiles wherever the carrier represents those coordinates
  + function validates complete metadata scope equality for coordinates not physically represented by carriers
  + function independently rederives both carriers then runs exact current reconciliation
  + only Equal releases one Serialize-only ClaimOnly result retaining both complete carriers
  + Different and every structural or rederivation fault remain distinct

& [SerdeBoundaryCandidate] is exact
  + only metadata forms and plans require strict Deserialize round trip
  + carrier bindings reconciliation outputs and evidence outputs are Serialize only
  + every serialized struct denies unknown fields
  + no aliases defaults flattening untagged enums catch-all maps arbitrary JSON or compatibility fallbacks
  + no public constructor deserializer Default From Into or mutable field creates a carrier binding or output
  + compile-time trait assertions must prove metadata implements Deserialize and every carrier-bearing type does not

& [CarrierIntegrity] is stronger than transport convenience
  + complete current carrier values are passed by ownership not reconstructed from bytes
  + current output-only boundary remains byte and API unchanged
  + no digest-only loose assembly locator key lookup resolver or caller summary is accepted
  + no new stable reference profile is selected

& [ScopeJoinDecisionRequired] remains for SJS
  + exact carrier-exposed joins are encoding profile complete TopologyScanLimits root relative path root kind root volume serial root file identity assembly profile digest profile and reconciliation profile
  + candidate root reference root volume-GUID path closed policy digest predecessor admission profile and predecessor receipt digest remain metadata coordinates compared exactly between roles
  + SJS must state which coordinates are carrier-derived versus metadata-only without implying physical origin
  + no metadata-only coordinate may be omitted defaulted or called carrier-proven

& [FaultCorrection] is bounded
  + add no new broad fault family
  + metadata decode and trait mismatch route through InvalidRequest Profile Scope Lineage or Resource as appropriate
  + impossible carrier Deserialize is a static API denial not a runtime fault
  + one failure returns no partial binding reconciliation or result

& [TestCorrection] is mandatory
  + round trip every metadata input form and plan
  + serialize but statically refuse Deserialize for every carrier-bearing binding and result
  + prove current digest carrier remains non-Deserialize and byte unchanged
  + adversarially swap carriers between role metadata and refuse mismatched exposed scope
  + retain all prior identity scope lineage order reconciliation Different Equal ClaimOnly grade and effect-absence cases

& [CandidateDiff] remains bounded
  + at most one safe pure module and one lib export after a new exact signature
  + existing dependencies only and no Cargo or lockfile change
  + no modification to current carrier modules
  + no unsafe platform filesystem clock producer issuer consumer or effect surface

& [RequiredReprocessing] is complete
  + audit this source against prior source fault invalidation current carrier code and locked historical artifacts
  + revise exploded refinement constraint data design canonical matrix plan slice phase lock proof review and signature generations where trait closure changes meaning
  + add an explicit Rust trait-feasibility gate before any replacement signature
  + preserve all prior bytes as historical invalidated authority

& [AuthorityState] is raw corrective source only
  + canonical authority is absent
  + implementation authority is absent
  + execution authority is absent
  + physical producer issuer consumer provider persistence remote FPGA and Minecraft authority are absent
  + next is source manifest and multi-authority corrective SJS input audit
