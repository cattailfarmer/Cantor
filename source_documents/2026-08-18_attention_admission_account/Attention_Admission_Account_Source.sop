Subject: Attention Admission Account Source

Description: Preservation of the next Cantor runtime need after declared-field binding: an inference-callable route result should make deterministic host admission legible without disclosing argument values or confusing a learned selection with authorization.

& [SourceSnapshot] is immutable
  + [source_snapshot_uuid] is 2954f6ef-aead-46d0-ad10-9df4da63887c
  + [conversation_uuid] is cab190f5-eda0-44e0-81a5-51f9605e0baa
  + [captured_at] is 2026-08-18T11:55:40.0838292-05:00
  + [baseline_commit] is d9e4fd5b85d6408c9136b1bbeac740050b237580
  + [source_status] is preserved_observed_unprocessed

& [ObservedGap] follows the working runtime
  + successful route output identifies procedure digest arguments and Needle confidence
  + immutable evidence contains caller input and raw learned selection
  + deterministic schema grounding declaration catalogue-recheck and effect gates are implicit in control flow
  + an operator or LLM tool caller cannot inspect which declaration surface activated without reconstructing controller behavior

& [CandidateAccount] is non-authoritative until SJS
  + emit one versioned account after every successful route admission and before semantic execution
  + bind it to procedure catalogue and caller stimulus identities
  + name required argument fields but never echo values
  + state whether each field was explicitly declared or remained literal-grounded
  + distinguish literal-only delimited and whole-document JSON surfaces
  + state schema grounding binding catalogue-recheck and effect gate dispositions
  + make the account available in route-only and full success results and immutable evidence

& [TrustBoundary] remains fail closed
  + an account records completed host checks and does not perform or authorize them
  + no account exists for a rejected call because the typed fault is the rejection account
  + account construction must not invoke Needle Cantor llama.cpp or external effects
  + account bytes must be deterministic for the same identities and gate facts
  + procedure arguments and caller stimulus content must not appear in the account

& [CompatibilityIntent] is additive
  + existing result fields evidence verifier evaluator calibration and semantic frame remain valid
  + no catalogue procedure source threshold model or shared service change is implied
  + the smallest useful output is preferred over a general trace or private reasoning log
