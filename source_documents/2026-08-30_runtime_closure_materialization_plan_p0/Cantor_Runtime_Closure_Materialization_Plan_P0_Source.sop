Subject: Cantor Runtime Closure Materialization Plan P0 Source Snapshot

@ [source_snapshot_uuid] dc7d8d88-83bd-4f0c-bc2a-6973afa99700
@ [conversation_uuid] 01a02268-2614-7d80-9737-ea77f4aeacb1
@ [captured_at_utc] 2026-08-30T17:58:16.7176363Z
@ [repository] C:\Project\Cantor
@ [branch] codex/self-hosted-corpus

& [DictatedIntent]
  + the user wants the Cantor runtime plus Cantor SOP to be the compact initiating seed from which a complete selected Cantor runtime can be inflated
  + the user wants Cantor to run from SOP, perform its own governed work and updates, and eventually generate its succeeding SOP rather than remain only a documentation surface
  + the current twelve-hour goal directs continuous highest-leverage provider-free work when live provider model remote host hardware or physical authority is absent

& [EstablishedPredecessor]
  + RIS-P0 Revision 0.2 canonical 9f2b4613-353f-4cf2-ab66-a3bb3b97feb3 compiles one supplied two-root runtime closure declaration into a deterministic effectless plan
  + implementation aa71a1c729e59a57b98f2dc20e28a18f3e701350 and publication bookend 611cd4a0d46ba87c8126c8a75bd8f8c074645634 are remote equal and post-push replayed
  + P0 proves declaration compilation only and performs no prerequisite resolution source reacquisition build staging verification layout receipt or installation action

& [SelectedNextSeam]
  + form a provider-free Runtime Closure Materialization Plan P0 compiler before any RIS-P1 executor
  + input is one exact valid Revision 0.2 runtime-closure envelope supplied as bounded canonical bytes and independently replayed before planning
  + output is one deterministic ordered materialization work plan and verification account with execution_authorized false observation_count zero action_count_executed zero and all effect counters zero
  + the plan makes future prerequisite source build stage target verify layout rollback and receipt-candidate work explicit without performing or authorizing it

& [PlannedOperationSurface]
  + seed-root validation candidates bind the bootstrap-runtime and installation-SOP root identities expected hashes sizes targets verifier profiles and executable dispositions
  + prerequisite-resolution candidates bind every unresolved P0 prerequisite identity kind reference and reason without reading a host secret network reservoir or environment
  + nonroot material candidates remain distinguished as deterministic transform source build content-addressed acquisition explicitly supplied material or generated configuration
  + target-stage and material-verification candidates bind each node to its exact portable logical target expected hash expected bytes verifier and dependency order
  + closure-finalization candidates describe layout verification rollback preparation and installation-receipt candidate production while asserting no observed installed or activated state

& [OrderingAndIdentity]
  + operation identities and order derive only from exact P0 request plan and envelope identities, not ambient path filesystem order cache state clock or caller order
  + prerequisites precede dependent source or material work; material production follows P0 topological order; stage precedes verify; all node verification precedes layout rollback and receipt-candidate stages
  + every plan request plan operation verification and retained evidence digest is deterministic domain-separated and self-excluding

& [AuthorityAndDelineation]
  + materialization_plan means an effectless proposed work order, not materialization execution
  + planned filesystem reads writes directory creation replacement permission changes process compiler package-manager network artifact provider model MCP Git workspace service activation cleanup rollback remote hardware and external effects remain denied
  + a planned action proves no material presence source availability provenance license policy trust compatibility safety reproducibility successful build installed layout verifier result activation or receipt truth
  + private keys credentials tokens and other secret values remain external custody and may be referenced only through nonsecret opaque identities

& [AcceptanceDirection]
  + strict bounded forms reject unknown duplicate overbound stale wrong-profile noncanonical and trailing machine input
  + exact P0 replay refuses any changed request plan receipt denial lifecycle authority digest or fully redigested semantic widening before materialization planning
  + semantic permutations that preserve the same P0 closure produce byte-identical materialization plans
  + one small synthetic provider-free fixture independently double-replays retained request envelope verification and manifest bytes in debug and overflow-checked release
  + adversarial tests remove reorder duplicate or mutate prerequisite material source target operation denial observation authority and receipt-candidate fields and fail closed with typed faults

& [StillSeparate]
  + a real Cantor bootstrap executable a real installation SOP closure declaration supported host and artifact-pack profiles actual source or model bytes and every license or operator acceptance remain separately supplied and signed
  + any RIS-P1 executor physical filesystem process acquisition build staging verification permission rollback or receipt effect requires a later explicit commission and effect evidence
  + activation boot health successor recognition self-update provider inference shared attention remote deployment FPGA and Minecraft remain outside

& [AuthorityBoundary]
  + this snapshot authorizes source custody and later SJS formation of a provider-free plan compiler only
  + no canonical specification satisfaction signature Rust implementation material observation acquisition build write installation activation cleanup remote operation or physical effect is authorized
