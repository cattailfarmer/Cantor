Subject: EVO-X2 Cantor Plan-Only Build Job P0 Source

@ [source_uuid] 478dfde1-0a78-4d61-9bae-c3db82174d93
@ [source_snapshot_uuid] e0045953-7c43-47bd-8c83-f40b8ff84cf5
@ [conversation_uuid] 01a02268-2614-7d80-9737-ea77f4aeacb1
@ [captured_at] 2026-09-08T16:20:46.6760000-05:00
@ [capture_kind] user_dictation_plus_completed_host_pilot_evidence_plus_agent_bounded_successor_design

& [UserDictation]
  + "we should set up EVO-X2 as a Cantor host and start running our build through the Cantor process and see how well it performs."

& [PredecessorEvidence]
  + EVO-X2 Current Build Host Pilot P0 canonical4cf12917-73e8-4505-9228-f94d981143bd is complete and published at immediate bookend d805681daee0d6846574875e25b6e1e41c9207bc
  + current package36 artifacts8137558 bytes is installed under C:/AI/services/cantor-current-pilot-48479932 and two fresh A8 verifier processes passed in136ms and32ms with byte-identical23018-byte output
  + exact protected historical roots pinned loopback provider model process command listener and host configuration remained unchanged; no pilot process service or startup remains
  + P0 intentionally did not compile source or run the current query lane because its complete in-root public dependency closure was unavailable

& [Problem]
  + the host now proves current Windows binary portability and deterministic evidence replay but has not yet shown Cantor representing a real Cantor build as a governed executable work object
  + jumping directly to source transfer toolchain installation scratch compilation or workspace mutation would combine planning prerequisite discovery physical effects and performance measurement into one ambiguous experiment
  + historical C-drive self-work planners are bound to older repository commits paths Git identities and machine contracts and must not be relabeled as a current EVO-X2 build plan

& [Objective]
  + implement the smallest current Rust plan compiler that turns one exact build-job request into a deterministic typed effectless build plan
  + package and run that compiler as a Cantor process on EVO-X2 twice in fresh processes from a new immutable isolated root
  + independently verify request plan output identity determinism non-authority and performance after retrieval
  + use the result to decide the exact prerequisites and effect boundary for a later scratch source build without granting that later authority here

& [PlanCompilerBoundary]
  + new binary profile is cantor-evox2-plan-only-build-job/0.1 and accepts exactly one bounded nonlink UTF-8 JSON request file
  + the compiler reads only its request beneath the isolated package root and writes only canonical JSON to stdout; it performs no source workspace Git toolchain environment provider network or host inspection
  + output is a declarative plan not a shell script and contains no executable command string that the operating system can invoke directly
  + every future operation declares a typed executable identity placeholder ordered argument atoms working-directory placeholder input identities output identities preconditions denials and evidence expectations
  + all path values are portable forward-slash absolute target declarations under exact future roots and are treated as inert data

& [RequestShape]
  + request has exactly16 ordered fields: profile request_uuid predecessor_canonical_uuid predecessor_bookend_commit source_commit source_archive_sha256 repository_remote_ref target_host target_os future_workspace_root future_target_root cargo_profile requested_checks bounds authority_denials request_sha256
  + requested_checks is exact ordered set workspace_debug workspace_release workspace_clippy workspace_format and no duplicate omission reorder or unknown check is accepted
  + bounds has exactly6 ordered fields maximum_archive_bytes maximum_files maximum_stdout_bytes maximum_operation_count timeout_seconds cargo_build_jobs
  + authority_denials is an exact closed set including source_transfer source_extract toolchain_install dependency_fetch workspace_write process_execute commit push provider_call service_change and external_network
  + request fixed-domain self digest covers canonical bytes with request_sha256 empty

& [PlanShape]
  + plan has exactly20 ordered fields: profile plan_uuid request_uuid predecessor_canonical_uuid predecessor_bookend_commit source_commit source_archive_sha256 target_host target_os future_workspace_root future_target_root cargo_environment operations operation_count expected_evidence authority_grants authority_denials unresolved disposition plan_sha256
  + cargo_environment fixes CARGO_NET_OFFLINE true CARGO_BUILD_JOBS1 CARGO_INCREMENTAL0 RUST_TEST_THREADS1 RUST_MIN_STACK33554432 and contains no ambient interpolation
  + operations are exactly seven ordered typed records: verify_source_archive materialize_absent_workspace workspace_debug workspace_release workspace_clippy workspace_format seal_build_receipt
  + each operation has exact bounded fields and dependency ordinals forming one acyclic sequence; operation_count is7
  + authority_grants is empty unresolved includes source_not_transferred workspace_not_created toolchain_not_observed commands_not_executed and performance_not_measured
  + disposition is planned_effectless and the fixed-domain plan digest covers canonical bytes with plan_sha256 empty

& [ImplementationAndVerification]
  + implement pure typed forms compiler serializer parser and independent verifier in the current Cantor Rust workspace with no new dependency
  + compiler derives every field except request-carried identities from constants and deterministic domain-separated digests; wall time host state and randomness never enter plan bytes
  + verifier reparses request and plan independently recompiles expected semantics checks both self digests exact fields arrays bounds denials zero grants and output bytes
  + tests include exact roundtrip double compilation and refusals for raw-byte digest tamper field addition reorder path escape UUID change source hash change check reorder bound widening operation mutation dependency cycle authority grant unresolved removal and trailing content

& [EVOX2PackageAndRun]
  + create a new immutable final root C:/AI/services/cantor-build-planner-d805681d through a unique sibling stage; never modify merge or delete the completed P0 root
  + package only compiler verifier exact request package manifest and necessary public evidence; no source archive Cargo target model toolchain dependency cache or credential
  + verify exact manifest locally and remotely then run compiler exactly twice in fresh processes and verifier exactly once against each output
  + retrieve exact request two plans verification records receipt and final audit into a new experiment directory and independently verify without remote access

& [PerformanceMeasures]
  + record package bytes install duration compiler duration for each fresh run verifier durations output byte count output SHA256 and deterministic equality
  + compare only first versus second plan compilation on the same host and exact input; do not call this source-build throughput or agent productivity
  + actual build timing remains unmeasured because no Cargo process source materialization or compilation is authorized

& [HostConservation]
  + audit the completed P0 root three historical service roots and pinned provider identity before and after
  + configuration_changed=false listener_delta0 persistent_planner_process_count0 and no service task startup hook environment registry firewall or policy change
  + refuse final-root collision wrong host unsafe path manifest mismatch linked file changed protected identity or provider drift

& [Acceptance]
  + source custody canonical SJS signature implementation gates publication and immediate bookends precede new remote bytes
  + exact package installs at the new root without altering the completed P0 or historical roots
  + two fresh plan compilations and their independent verifications pass with byte-identical canonical output and zero authority grants effects provider requests or external network
  + retained evidence independently verifies on both PowerShell hosts and reports bounded performance honestly
  + result enumerates enough exact unresolved prerequisites to form the later scratch-build contract without claiming the build occurred

& [OutsideScopeAndFrontier]
  + no source checkout archive transfer extraction Rust or Cargo discovery toolchain install dependency fetch command execution scratch build workspace mutation commit push self-update provider call model inference service persistence or security-policy change
  + after successful plan-only P0 evidence a separately signed successor may transfer one exact Git archive and execute the plan in a disposable workspace with capacity guard rollback and no publication authority
  + autonomous Cantor work Needle or Needle2 semantic inference and shared-attention negotiation remain later product milestones
