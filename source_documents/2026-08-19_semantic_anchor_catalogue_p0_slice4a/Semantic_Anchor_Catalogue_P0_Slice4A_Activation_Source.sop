Subject: Semantic Anchor Catalogue P0 Slice 4A Activation Source

@ [activation_source_uuid] 66c38de6-6d9c-41af-99f3-31ea59ac43f6
@ [conversation_uuid] 019f94c8-da23-7131-aef2-6ac02612d155
@ [captured_at_utc] 2026-08-20T03:00:47Z
@ [user_direction] "set up another 4-hour work shift now. git er done!"
@ [canonical] C:\Project\Cantor\specifications\Cantor_Semantic_Anchor_Catalogue_P0.sop
@ [prior_proof] C:\Project\Cantor\proofs\Cantor_Semantic_Anchor_Catalogue_P0_Slice3_Proof.sop

& [PreservedDirection]
  + continue the user-requested Cantor work shift after publishing Slice2 and Slice3
  + select the next dependency-safe local milestone and keep production moving
  + preserve source SJS proof rollback and Git publication discipline

& [SelectedImplementationClaim]
  + refine canonical Slice4 into dependency-contiguous sub-slices and activate only Slice4AResultAccounts
  + complete the proof-carrying AnchorQueryResult shape with explicit typed relationship paths and a recomputable per-channel association account
  + add an optional typed relationship path directly to AssociationContribution so path order direction endpoints relation types and hops are machine data rather than parsed prose
  + define RelationshipDirection AnchorRelationshipStep and AnchorRelationshipPath closed forms
  + define AssociationChannelAccount as a deterministic index from one channel to exact candidate identities and contribution count
  + emit typed paths only for TypedRelation contributions and require every other channel to omit them
  + require every typed path to start at its declared seed chain exact step endpoints end at the candidate address match RelationHops and contain every step relation in evidence_refs
  + canonicalize and deduplicate paths independently from contribution order
  + derive the result relationship_path_set exactly from candidate contributions
  + derive the result association_account exactly by grouping retained and non-retained contribution records by their declared channels without translating channel-local values
  + commit both result fields under anchor_query_result_digest and whole-result scanner rebuild

& [Acceptance]
  + existing exact identity exact label applicability and typed relation scanner behavior remains semantically unchanged
  + forward and reverse typed relation results expose ordered path steps without parsing the human-readable basis
  + relation step endpoint discontinuity wrong direction wrong candidate wrong hop count missing evidence duplicate or noncanonical path fails closed
  + exact label identity applicability lexical embedding and learned contributions carrying relationship paths fail closed
  + association account channel order candidate identities and contribution counts exactly recompute from candidates
  + missing extra duplicate reordered or count-mutated association accounts fail closed
  + any path or account mutation changes result digest or refuses validation
  + equal scanner inputs produce byte-identical paths accounts result bytes and digests
  + existing catalogue scanner and full workspace tests warnings-denied lint format and evidence gates pass

& [GraduationBoundary]
  + Slice4A contains result-account machine forms validators scanner projection and adversarial tests only
  + Slice4B deterministic lexical contribution requires a later preserved source completed-work review and phase lock
  + Slice4C compact requested-detail record projection and direct-query equivalence requires a later preserved source completed-work review and phase lock

& [Boundary]
  + no lexical matching token scoring requested-detail content projection query equivalence embedding learned routing provider attention bridge persistence network remote hidden state effect FPGA OneDrive EVO-X2 or Minecraft behavior
  + no authority truth confidence risk or permission arithmetic
  + no package compilation signing trust mutation source admission or direct-query behavior change
  + direct Cantor query iterative-attention and Slice3 exact scanner semantics remain rollback surfaces

& [Rollback]
  + remove Slice4A forms fields validators scanner population tests governance solution proof and records
  + restore the published Slice3 AnchorQueryResult shape without package data migration or changes to admitted fabric
