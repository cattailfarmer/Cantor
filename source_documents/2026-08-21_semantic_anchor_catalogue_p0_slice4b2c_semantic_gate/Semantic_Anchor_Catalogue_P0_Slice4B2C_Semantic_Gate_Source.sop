Subject: Semantic Anchor Catalogue P0 Slice 4B2C Semantic Gate Source

@ [source_uuid] 6f9c3a5a-a0a2-4a96-b492-2fefec12c429
@ [captured_at_utc] 2026-08-21T00:00:00Z
@ [conversation_uuid] 019f94c8-da23-7131-aef2-6ac02612d155
@ [speaker] user continuation plus Codex bounded engineering articulation
@ [dependency] proofs/Cantor_Semantic_Anchor_Catalogue_P0_Slice4B2B_Proof.sop

& [PreservedUserIntent]
  + [verbatim] "let's do another 4-hour slice now"
  + [inherited_intent] ordinary terms may propose SOP anchors but semantic purpose use case boundary authority lifecycle and safety decisions remain mandatory and proof-bearing

& [Purpose]
  + join the completed lexical lookup to the existing exact scanner without changing either published result form
  + treat lexical matches only as candidate identity seeds and require the exact scanner to own every semantic eligibility decision
  + make the join provider-free deterministic bounded replayable and non-authoritative

& [JoinForm]
  + LexicallySeededAnchorGateRequest binds one validated lexical request result and one exact AnchorQuery
  + query known_identities equals the complete canonical set of lexical match unit identities
  + query allowed_channels is exactly ExactIdentity so lexical relevance cannot become a scanner contribution or priority tier
  + query term_set is empty and continuation is absent so no independent label or pagination discovery is mixed into the join
  + LexicallySeededAnchorGateResult binds lexical proof digest exact scanner input and result digests ordered lexical match identities scanner candidates boundary account fixed non-authority statement and a domain-separated join proof digest

& [Algorithm]
  + whole-validate lexical index lookup request and result against catalogue and fabric
  + require at least one lexical match and exact unique match identities
  + validate the exact scanner query and require request id catalogue fabric known identity channel term and continuation correspondence
  + run scan_exact_semantic_anchors with no continuation
  + require every lexical identity appears exactly once as a scanner candidate or explicit unknown account and no exterior identity appears
  + preserve scanner eligibility priority guards contributions boundary proof and omissions unchanged
  + enforce positive hard-capped match candidate and serialized result budgets before success
  + compute and whole-replay a domain-separated join digest

& [Acceptance]
  + eligible lexical seeds remain eligible only when exact gates pass
  + purpose use-case include-boundary exclude-boundary authority lifecycle protected-identity and unknown identity cases retain scanner dispositions
  + lexical coverage ordering cannot alter exact scanner priority or eligibility
  + changed lexical proof query roots known identities channels terms continuation scanner result or join proof refuses
  + equal inputs serialize byte-identically and no provider process filesystem network persistence effect or clock is consulted

& [Locked]
  + no scanner modification no lexical lookup modification no winner promotion no pagination no compact projection no CLI publication no provider embedding learned route persistence effect remote hidden state llama.cpp FPGA OneDrive EVO-X2 or Minecraft
