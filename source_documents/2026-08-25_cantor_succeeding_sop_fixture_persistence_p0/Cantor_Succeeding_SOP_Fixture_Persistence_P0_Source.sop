Subject: Cantor Succeeding SOP Fixture Persistence P0 Source

@ [source_uuid] 162eca3b-48bd-429d-80d1-fb1217a86830
@ [source_snapshot_uuid] 2c60682b-8233-46d0-8dbd-46c7c355b90b
@ [conversation_uuid] 01a02268-2614-7d80-9737-ea77f4aeacb1
@ [captured_at_utc] 2026-08-25T21:31:29.5312243Z
@ [predecessor_commit] 8cb80c86f88e5b4cd407a09146f01cabec6766a5

& [UserObjective]
  + resume the previous Cantor product-development goal for a five-hour work slice
  + advance from the published SWA-05 B1 checkpoint toward an enforceable local self-work host and then SWA-06B independent review signing persistence and activation where safely supportable
  + preserve the exact working tree foreign work provider boundary host policy requirement-linked evidence exact gates and verified publication

& [RecoveredState]
  + SWA-06A attributable succeeding-SOP proposal SWA-06B1 independent review-signature admission and SWA-06B2A activation-transaction admission are published
  + SWA-06B2A binds exact source reacquisition current registry atomic transition supersession rollback and recovery-owner plans but performs no physical operation
  + SWA-06B2B physical persistence remains the next product seam before any succeeding SOP can become current
  + the selected npm-native SWA-05 B1 host is still exact Codex 0.135.0 executable SHA256 FE12887B4AB4A4E988F0FA5BAAE9E5CB7D8505C26401378628E762DB9A2E798F and its selected readOnly schema still lacks allowed-read and denied-read scope controls
  + SWA-05 therefore remains a verified NotRun and no host identity policy or requirement is silently changed

& [SelectedSlice]
  + form SWA-06B2B1 as a synthetic-fixture-only physical source reacquisition and current-registry persistence kernel
  + consume and fully revalidate one exact SWA-06B2A receipt whose activation policy status is synthetic_fixture_only
  + require an explicit disposable fixture marker commission recovery owner and root identity before touching the filesystem
  + reacquire the exact raw source bytes verify byte count and SHA256 observe the exact predecessor registry record write and flush one same-parent temporary successor atomically replace the registry flush its parent reopen and verify the successor
  + emit a self-digested receipt proving only the bounded fixture operation

& [FixtureAuthority]
  + the caller supplies one canonical fixture-root path to the Rust function and no environment current-directory discovery or command execution is available
  + the root must preexist be a real directory carry one strict self-digested marker be disposable and deny live repository and live activation use
  + the marker fixture-root identity and recovery-owner identity must equal the commission and activation transaction
  + a .git entry at the root is an unconditional refusal
  + the source registry and temporary paths are inherited exactly from the validated B2A transaction and remain repository-relative beneath their governed top-level directories
  + every existing root source registry parent registry leaf and marker component is checked against symbolic-link or Windows reparse substitution before use

& [RegistryRecord]
  + the physical registry is one strict self-digested machine form embedding the exact current-registry snapshot plus fixture-root activation-authority recovery-owner and last-transaction identities
  + the predecessor record must project byte-for-byte semantic equality with the B2A current-registry snapshot before any write
  + the successor record increments exactly once selects the independently reviewed proposal revision digest and source path and binds a separately supplied successor snapshot identity
  + the predecessor source is preserved and the source document is never rewritten normalized moved or deleted

& [WriteProtocol]
  + reject an existing temporary path before contact
  + serialize one deterministic successor record with LF newline
  + create_new the exact same-parent temporary file write all bytes flush the file reopen and compare exact bytes
  + use safe standard-library same-volume rename replacement for the exact final path
  + flush the registry parent directory reopen the final record validate its digest and compare exact successor bytes
  + leave no temporary file after success and never clean up the caller-owned fixture root

& [Receipt]
  + bind exact commission marker activation-transaction source predecessor successor and physical byte identities
  + report physical_contact source_reacquired registry_observed registry_persisted and current_sop_selected true only after final reopen verification
  + report boot_activation_verified rollback_executed live_activation_performed provider_contacted model_called process_launched network_contacted and cleanup_performed false
  + authority is synthetic_fixture_persistence_only and status is fixture_registry_persisted_awaiting_boot_validation
  + deterministic validation and strict bounded machine forms deny unknown fields and digest laundering

& [FaultAndRecoveryBoundary]
  + typed refusals cover upstream marker commission identity root path source registry temporary path link or reparse content digest precondition persistence post-write durability receipt bound and machine-form failures
  + before replacement any temporary file created by the current attempt is removed only when its exact create_new ownership is known
  + after replacement a failed final verification is reported without claiming rollback because independently verified rollback execution is a later SWA-06B2B2 slice
  + no boot validation SOP interpretation process launch provider model or live activation is performed
  + no production Cantor Pinky or other repository is a valid fixture target

& [ImplementationShape]
  + one safe Rust module in cantor_ecosystem reuses the unmodified cantor_core SWA-06B2A validator
  + production code may use only explicit path filesystem serialization and digest surfaces and has no environment clock process network provider model unsafe Git cleanup or remote surface
  + focused tests use recovery-owned temporary fixture directories and verify deterministic success stale registry source tamper marker substitution existing temp reparse or symlink and receipt mutation refusals
  + no CLI is formed in this slice so physical execution remains an explicit embedding decision rather than an ambient command

& [EvidenceAndStopRules]
  + source preservation input audit data design threat review seven-fold exhaustion canonical specification satisfaction signature phase lock and requirement matrix precede implementation
  + focused debug overflow-checked release warnings-denied Clippy format exact workspace recursive evidence attribution commit push and post-push replay are required
  + stop on any B2A replay failure externally_governed policy live repository marker live activation grant unsafe code unbounded path alias provider contact Windows policy bypass foreign contact or contradictory evidence
  + retain D:\CantorB1 without cleanup and retain pinned-provider unavailable with zero synthetic live trials

& [ForeignExclusions]
  + AGENTS.md remains foreign tracked work and must not be edited or staged
  + narrative/turns/1786911720465_minecraftgenie_scope_handoff_negotiation.sop remains foreign and untracked
  + Zeta Accountability Identity neural other-conversation output temporary and every unrelated dirty path remain excluded
