Subject: Derived B1 Private Execution Permit Reference Correspondence P0 Source

@ [source_uuid] fbca54b1-bf51-4bca-9bad-b9cb7b3a83bb
@ [source_snapshot_uuid] cdbd323b-c260-415e-9473-d32604242e54
@ [conversation_uuid] 01a02268-2614-7d80-9737-ea77f4aeacb1
@ [derived_at_utc] 2026-09-04T18:56:37.2620000+00:00
@ [capture_kind] agent_derived_dependency_contiguous_development_source_not_a_user_quote

& [SourceAuthority]
  + the active user-requested four-hour Cantor goal authorizes the highest-leverage dependency-contiguous seam after verified A6 closure
  + user approval permits process-scoped RemoteSigned for reviewed Cantor PowerShell scripts with no persistent policy change; it is development gate authority only
  + active SOP authority is C:/Project/Pinky/.sop with the current Cantor overlay; D:/crf is the selected implementation and publication surface
  + published A6 implementation cd7258ad0275722aba94caa541022c6d593a9fe8 and immediate bookend 77d42ee5169cde768126681b235e385cf3be558b precede this source
  + A6 implementation proof 17d6a930-d45e-4c98-98be-b7b87a190212 closes thirty-three requirements only for supplied expected-observation correspondence
  + do not inherit historical ReasoningFramework pointers as live authority or import Pinky code foreign worktrees private data provider substitutes or unrelated projects

& [DependencySelection]
  + published authority packet canonical c0460d30-4870-44fc-82d2-baf2bc5cc0f5 selects A7 ordinal seven authority_name private_execution_permit artifact_kind private_execution_permit_reference_candidate required_verifier_profile private-execution-permit-verifier/0.1 confidentiality secret_reference_only dependency ordinal six
  + its deterministic fixture descriptor has candidate UUID a1000000-0000-4000-8000-000000000007 opaque reference fixture_candidate_a7 declared bytes 7168 content SHA256 3b536ba8d52edd1c262873b5e58684b3fec70d1341e446bed25fbc51736789d1 and descriptor SHA256 876aad6d1d7ce8c0fb0c4c0e2a57b09c925f4498382a97aa912597526cbf58fd
  + retained packet.json is  raw SHA256 DAE6175BF3BA7A897BC6AACDC55DCB8A81F356047750536B8592325B1EC0446A and retained request.json is raw SHA256 72E897481A3438D1FC3027D62F8512F080E006F9C9720EBA0543EBE9E9581D21
  + A1 through A6 correspondence implementations do not prove any real-world authority named by their packet coordinates
  + after A7 broker_projection ordinal eight and physical_preparation ordinal nine remain unresolved; no descriptor other than A7 may change

& [ProblemAndObjective]
  + A6 proves deterministic correspondence between supplied observations and inherited expectations but explicitly leaves private_execution_permit_present false
  + implement a provider-free reference-correspondence adapter that replays full A6 and compares an exact bounded supplied A7 reference envelope to the unchanged ordinal-seven descriptor
  + emit a deterministic descriptive matched-or-mismatched receipt without opening resolving reading authenticating consuming or exercising the referenced permit
  + preserve secret_reference_only by keeping all fixture evidence synthetic and by admitting no raw permit payload private key credential bearer token filesystem path URI or resolver capability
  + this is not permit verification admission issuance custody revocation expiry freshness authorization consumption broker projection preparation or runtime activation

& [UnchangedReuseAndHistoricalPins]
  + reuse the complete unchanged A6 verifier and compare the entire supplied A6 receipt before A7 processing
  + recompile the current packet twice and require byte identity; current request may differ from A6 authority_packet_request only at ordinal seven
  + all prior and subsequent descriptor bytes dispositions unresolved-authority order effect account subjects and attempt limits remain conserved
  + retain all historical commits request digests plan namespace decision lineage observation values and A6 mismatch semantics exactly; A7 cannot reinterpret or repair them
  + the reference envelope binds the exact replayed A6 receipt self-digest and A7 descriptor self-digest

& [ReferenceEnvelope]
  + create a strict canonical profile cantor-b1-private-execution-permit-reference-envelope/0.1
  + ordered public metadata fields are profile envelope_uuid a6_receipt_sha256 candidate_uuid authority_name artifact_kind opaque_reference content_sha256 declared_bytes confidentiality required_verifier_profile fixture_only dependency_ordinal input_class evidence_references envelope_sha256
  + envelope UUID is canonical nonnil and distinct from source canonical formation signature packet and predecessor identities
  + candidate UUID authority name artifact kind opaque reference content digest declared bytes confidentiality verifier profile fixture flag and dependency ordinal exactly repeat the packet descriptor as supplied comparison data
  + input_class is deterministic_fixture_candidate or externally_supplied_candidate and must agree among request envelope descriptor origin and fixture flag
  + opaque_reference is bounded safe opaque text; it must never be interpreted as a path URI environment variable command provider handle credential or lookup key
  + content_sha256 is public reference metadata copied from the descriptor; matching it does not show that any secret material exists or has that digest
  + declared_bytes is metadata only and never authorizes reading that number of bytes
  + one through forty-eight bounded unique nonempty evidence references are inert strings only and are never resolved
  + envelope self-digest clears only envelope_sha256 and uses fixed domain cantor.b1.private-execution-permit-reference.envelope.v1

& [RequiredInputsAndRequest]
  + all explicit A6 inputs in their unchanged order plus complete a6_receipt.json
  + exact raw permit_reference_envelope.json as the A7 descriptor candidate
  + current verification_request.json binds source and formation anchors published A6 implementation bookend and proof full A6 input and receipt identities current packet request and packet identity exact A7 candidate raw and semantic identity input class evidence references and attempt limits
  + exact raw envelope bytes and SHA256 are verified before parsing; no normalization trimming case folding aliasing or hidden reference resolution
  + a coherent explicitly rebound synthetic reference under a new request is a different descriptive event, never proof of a real private permit

& [CorrespondenceCircuit]
  + compute fixed Boolean results for predecessor receipt descriptor packet raw-envelope self-digest candidate UUID authority name artifact kind opaque reference content digest declared bytes confidentiality verifier profile fixture flag dependency ordinal input class and evidence-reference correspondence
  + formation must decide the smallest exact ordered comparison set without collapsing raw identity descriptor identity or predecessor identity
  + mismatch reasons are duplicate-free exact ordered identifiers for false comparisons; all_correspondence_matches is true iff every required comparison is true and reasons are empty
  + both matching and mismatching well-formed envelopes produce descriptive receipts; malformed coordinates noncanonical shapes and forged summaries refuse
  + validly rehashed changes may describe a different explicitly rebound candidate only when every binding is changed coherently; that event remains supplied reference correspondence only

& [ReceiptTruthAndProfiles]
  + introduce only the smallest new envelope request receipt and evidence profiles; imported A6 types retain their existing grammar bounds and field order
  + receipt status distinguishes supplied_private_permit_reference_correspondence_matched_execution_unresolved from supplied_private_permit_reference_correspondence_mismatched_execution_unresolved
  + authority is supplied_private_permit_reference_correspondence_only in both statuses
  + receipt nests the entire reconstructed A6 receipt and preserves every upstream adverse state false authority field mismatch account and zero effect field
  + private_execution_permit_reference_correspondence_proved may describe exact supplied metadata comparison; private_execution_permit_present permit_material_authenticated permit_signer_identity_proved permit_signer_authority_proved permit_scope_proved permit_subject_proved permit_freshness_proved permit_nonexpired permit_revocation_checked permit_single_use_proved permit_unconsumed permit_consumed live_authorization_admitted ready_for_physical_execution and execution_authorized remain false
  + the unchanged twenty-two-field effect account remains entirely false or zero; maximum attempts one retry zero cleanup zero
  + request receipt and evidence self-digests clear only their own digest fields and use separate fixed A7 domains selected by formation

& [SecretReferenceBoundary]
  + public repository fixtures use only the already-published synthetic fixture_candidate_a7 coordinate and deterministic synthetic successor variants
  + no test fixture retained log error receipt narrative graph command line or environment value contains a real permit secret credential token private key or resolvable private locator
  + production parsing accepts metadata bytes only and exposes no callback trait path opener URI client network client environment reader process launcher or secret store integration
  + errors identify bounded field coordinates and refusal classes without echoing rejected opaque-reference or evidence-reference values
  + evidence manifests may hash public synthetic files but must not claim those hashes authenticate private material

& [EvidenceSurface]
  + formation must enumerate the exact retained file count and explicit input order by importing all A6 evidence and adding only the A7 request envelope receipt and manifest surfaces
  + evidence manifest binds every retained artifact by exact path bytes and SHA256 before semantic parsing; verifier independently reconstructs A6 and A7 twice without importing producer code
  + canonical compact UTF-8 typed field order exactly one retained transport LF raw candidate hashes exclude that LF and manifest artifact hashes include it
  + retain one MiB per form sixteen MiB aggregate depth thirty-two total object fields four-thousand-ninety-six general text eight-thousand-one-hundred-ninety-two bytes and references one through forty-eight unless formation proves a stricter bound
  + reject missing extra duplicate unordered traversal absolute symlink junction reparse nonregular oversized and digest-mismatched evidence
  + at least two fresh processes per debug and overflow-checked release profile reproduce exact receipt bytes; rehashed false retained identity reaches typed refusal
  + only an ignored test-owned producer may create deterministic public fixture evidence in a fresh caller-specified D-drive directory

& [AdversarialRequirements]
  + tamper every A6 raw input nested receipt decision observation comparison truth and effect field after rehashing untrusted outer layers and refuse broken predecessor replay
  + tamper each A7 descriptor coordinate envelope raw byte self-digest request binding comparison result mismatch ordering false authority attempt count and effect field independently
  + test empty duplicate reordered unknown trailing concatenated noncanonical overlong control-character path-like URI-like command-like and secret-shaped references without resolving or echoing them
  + distinguish well-formed mismatch from malformed reference shape raw-byte substitution forged positive receipt and coherent explicitly rebound synthetic candidate
  + cover both input classes both A6 match states inherited decision and interval states all single mismatches all-match all-mismatch and bounded feasible mismatch subsets
  + prove with explicit test instrumentation that reference and evidence strings never reach filesystem network environment subprocess provider model MCP signer key loader or broker APIs

& [ThreatModel]
  + prevent confused-deputy promotion where exact metadata is mistaken for permit validity authority or consent
  + prevent reference smuggling through path URI shell metacharacter Unicode control or log-forging text
  + prevent digest laundering where descriptor content_sha256 is represented as authentication of inaccessible material
  + prevent secret exfiltration through receipt echo error output retained evidence narrative or SJS graph
  + prevent time-of-check claims because no referenced object is opened atomically or otherwise
  + prevent predecessor truncation descriptor substitution field-order ambiguity permissive parsing and forged nested positive summaries
  + residual risk remains that a caller lies about supplied metadata; the receipt must name that limitation directly

& [EffectBoundary]
  + pure production comparison over typed supplied bytes; bounded evidence CLIs may read only explicit supplied public synthetic files and write canonical receipt plus LF or bounded nonsecret refusal to stderr with exit two
  + no live Git enumeration host observation clock filesystem target access environment secret provider model MCP network subprocess workspace write ref creation lease activation cleanup permit use or physical operation
  + development tests and D-drive builds are outside the runtime zero-effect account and remain scoped verification activity
  + no WSL remote machines download provider fallback host-policy weakening or persistent execution-policy change; provider_unavailable with zero live and synthetic provider trials remains preserved
  + C:/Project/Cantor dirty tree Pinky code other worktrees stashes D:/abe protected Minecraft narrative and unrelated build outputs are excluded

& [GovernanceAndAcceptance]
  + preserve and publish this derived source custody before complete canonical SJS formation and before any A7 Rust
  + complete input audit requirements constraint ledger delineation constructive and adversarial review data design primary research threat analysis sevenfold canonical and exploded specifications justification solution plan requirement matrix phase lock proof readiness satisfaction signature and independent dual-host frozen-formation gates
  + satisfaction protocol is ad10f10f-d506-48ef-a805-f8b0a133766c; formation must enumerate every ordered new field comparison mismatch reason imported A6 contract and false claim
  + exact focused debug overflow release locked-offline serialized workspace profiles documentation warnings-denied Clippy format scripts evidence attribution non-force publication direct equality and immediate bookends precede implementation completion
  + frozen A6 source signatures implementation proof code and evidence remain immutable; corrections require additive successor records
  + no test can promote matching synthetic reference metadata into execution authority

& [DelineationAndFrontier]
  + inside: source-bound full A6 replay exact ordinal-seven descriptor conservation bounded public synthetic reference metadata comparison deterministic mismatch accounting independent evidence and nonauthorizing receipts
  + boundary: an opaque reference and a digest are descriptions of a candidate coordinate not the candidate permit its custody its signer its validity its freshness its availability or permission to use it
  + outside: real private permit content resolution authentication issuer trust scope subject revocation expiry nonce single-use consumption live authorization broker projection physical preparation or autonomous operation
  + after source custody publication perform complete SJS; after formation signature implement only the declared provider-free reference contract; real permit access requires a later separately dictated confidential authority and runtime design
