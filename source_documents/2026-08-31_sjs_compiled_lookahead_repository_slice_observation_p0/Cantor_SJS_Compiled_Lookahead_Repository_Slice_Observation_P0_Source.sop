Subject: Cantor SJS Compiled Lookahead Repository Slice Observation P0 Source Snapshot

@ [source_snapshot_uuid] e4ca7100-5a6f-4276-8797-e5e79395720c
@ [conversation_uuid] 01a02268-2614-7d80-9737-ea77f4aeacb1
@ [captured_at_utc] 2026-08-31T17:10:55.5052222Z
@ [repository] C:\Project\Cantor
@ [branch] codex/self-hosted-corpus
@ [predecessor_commit] 76df960c9283fa2739f6b62ae4a664867a695ca7
@ [parent_extraction_source_uuid] 81ba4e67-0ebe-41db-bb8f-2437bc629c4c
@ [parent_extraction_canonical_uuid] 3359fdaf-f4bf-44f0-9892-3f8d8d5e027f
@ [parent_extraction_completion_signature_uuid] c14b101c-5e52-4ef6-927a-729381f95a2e
@ [parent_extraction_implementation_commit] da6af656378cb4c6538f215c4006f810a32dcac7
@ [parent_extraction_bookend_commit] 76df960c9283fa2739f6b62ae4a664867a695ca7

& [ConversationAttribution]
  + user commissions a four-hour work turn and asks continuous progress under the existing Cantor goal
  + published repository-candidate extraction reentry selects a separately sourced smallest bounded read-only physical repository acquisition adapter
  + project-manager articulation chooses exact Git commit-tree observation because it supplies missing physical content correspondence without semantic inference
  + no user statement is reclassified as this exact implementation design

& [Purpose]
  + close the smallest physical-observation boundary between one already-formed SjsRcxRequest and the exact immutable Git commit tree it claims to describe
  + verify repository branch commit and named blob identities while preserving the parent supplied-unobserved request unchanged
  + keep physical byte observation separate from candidate formation semantic labeling scoring coverage and selection

& [P0Input]
  + one RepositorySliceObservationRequest retains one fully sealed parent SjsRcxRequest whose input class is supplied_unobserved_repository_slice
  + request binds one canonical repository root one absolute Git executable expected Git executable SHA256 expected branch ref expected HEAD object format and bounded observation limits
  + parent request remains limited to one through sixteen records one through sixty-four obligations up to two hundred fifty-six explicit relations and up to sixty-four references
  + each parent record already supplies its exact safe repository-relative locator content SHA256 semantic declaration metrics and coverage correspondence

& [IdentityModel]
  + Git executable is a canonical regular file no-follow hashed before and after observation and must equal one supplied lowercase SHA256
  + repository root is canonicalized no-follow and must equal both the observation request root and the forward-slash repository identity retained by the parent scope
  + branch identity is an exact refs/heads path and its branch suffix must equal the parent scope branch
  + HEAD is the supplied lowercase object-format identity and remains exact before and after observation
  + parent scope commit digest must equal lowercase SHA256 of the exact raw commit-object content emitted by hash-pinned git cat-file commit HEAD
  + every record content digest must equal lowercase SHA256 of the exact raw regular blob content emitted by git cat-file blob for its ls-tree object identity

& [ReadOnlyGitProtocol]
  + one absolute hash-pinned Git executable runs with cleared environment C locale disabled system and global config terminal prompts optional locks lazy fetch replace objects attributes and file protocol
  + identity commands are version build-options rev-parse show-toplevel symbolic-full-name HEAD HEAD show-object-format absolute git-dir and absolute index path
  + exact parent locators are passed only after double dash to ls-tree -rz --full-tree HEAD and every returned record must be one ordinary blob mode 100644 or 100755 with exact path identity
  + each unique blob object is read through cat-file blob by validated object identity and the commit object through cat-file commit by validated HEAD identity
  + HEAD branch Git executable and index SHA256 are sampled before and after and any drift refuses the whole observation
  + stdout stderr command count commit bytes per-blob bytes total blob bytes path bytes record count index bytes and machine forms are closed bounded

& [OutputAndAccounting]
  + receipt retains the exact observation request the unchanged parent request repository branch HEAD object format Git version and executable identity
  + one observation account per record binds element candidate locator tree mode object identity exact raw bytes SHA256 and correspondence status
  + receipt binds command count commit byte count total blob byte count unique blob count index before and after identity and pre/post stability
  + verified observation may call the already-published parent compiler only after every record correspondence passes and retains the unchanged parent downstream envelope and verification
  + physical_contact is true filesystem and process observation are explicit while repository write index write worktree write network provider model inference prompt stitch secret permission remote-hardware and external-effect authority remain false

& [RefusalAndAdversaries]
  + refuse parent synthetic input unknown or relative Git executable executable hash drift repository symlink or root mismatch branch detached or mismatch HEAD mismatch object-format mismatch commit digest mismatch and pre/post identity drift
  + refuse locator not returned duplicate tree path tree path outside supplied set nonblob symbolic link submodule unsupported mode malformed NUL tree record invalid object identity missing blob overbound blob duplicate element or candidate identity and raw blob digest mismatch
  + refuse duplicate unknown noncanonical trailing over-depth over-field and oversized request receipt verification or retained evidence JSON
  + refuse any stderr nonzero process status timeout output overflow command overflow index drift executable drift or unexpected physical effect account
  + adversarial fixtures cover dirty working-tree contrast to prove committed blob selection and never reinterpret working-tree bytes as committed evidence

& [BoundedFixtureDirection]
  + script creates one disposable local Git repository outside the Cantor source tree with eight exact regular committed blobs matching the parent fixture locators and one additional dirty working-tree contrast
  + fixture derives parent content digests from committed raw blobs only seals a supplied_unobserved_repository_slice request and observes it through the hash-pinned local Git executable
  + expected unchanged parent result remains eight records six obligations twelve edges ninety-two subsets three selected five rejected one dominated zero uncovered and fourteen zero parent effects
  + observer receipt separately reports physical contact and exact read-only process counts without laundering those observations into parent zero-effect claims
  + retained evidence and an independent verifier replay typed correspondence and raw retained bytes; live fixture creation is never presented as current Cantor repository observation

& [UseBoundary]
  + P0 observes only one supplied exact commit tree and locators already declared by the parent request
  + P0 never lists the repository generally reads arbitrary working-tree content parses SOP prose infers a requirement generates semantics scores text discovers coverage estimates tokenizer truth or mutates parent fields
  + P0 does not stage commit push install hooks run filters contact remotes invoke a provider or model place a prompt activate a stitch continue autonomous work or write repository state
  + observation truth is limited to the pinned executable exact repository exact commit exact paths exact bytes and exact run receipt

& [AuthorityBoundary]
  + this snapshot authorizes source custody and later SJS formation only
  + no canonical contract satisfaction signature Rust implementation Git invocation filesystem read fixture creation repository observation compilation prompt provider model performance claim autonomy durable custody successor activation host mutation remote machine FPGA Minecraft or external effect is yet authorized
