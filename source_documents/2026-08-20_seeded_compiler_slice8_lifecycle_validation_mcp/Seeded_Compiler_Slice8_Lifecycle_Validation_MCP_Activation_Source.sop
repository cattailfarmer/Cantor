Subject: Cantor Seeded Compiler Slice 8 Lifecycle Validation MCP Activation Source

@ [source_uuid] 526cea59-fda0-47ff-9dac-12ccc7c59d39
@ [conversation_uuid] 019f94c8-da23-7131-aef2-6ac02612d155
@ [captured_at_utc] 2026-08-20T21:42:44Z
@ [Slice7_dependency] proofs/Cantor_Seeded_Compiler_Slice7_Lifecycle_Validation_Protocol_Proof.sop

& [PreservedContinuation]
  + the requested five-hour work slice remains active after Slice7 closure
  + Slice7 names a thin read-only MCP adapter as the next high-leverage seam

& [Purpose]
  + make validate_native_lifecycle_request callable as one ordinary model tool between inference passes
  + preserve exact Slice7 request response fault exit-equivalent and non-authority semantics
  + isolate this adapter in its own provider-free stdio server so no existing MCP tool list or runtime changes

& [Tool]
  + name validate_native_lifecycle
  + accepts one complete NativeLifecycleValidationRequest as request
  + rejects missing extra malformed and oversized arguments before dispatch
  + returns NativeLifecycleValidationResponse as structuredContent and only a short bounded human summary as text
  + marks lifecycle and input refusal as MCP tool error while retaining the complete structured response

& [Metadata]
  + exactly one tool is listed
  + annotations are read_only true destructive false idempotent true open_world false
  + instructions say coherence is not truth safety passage permission or effect authority
  + schema may keep the nested lifecycle request opaque to MCP metadata in P0 to avoid consuming model context with the complete compiler form graph; runtime deserialization remains fully strict

& [Runtime]
  + stdio only, no network listener, state, registry, model call, process child beyond the MCP host launching the server, filesystem, clock, signing, runner, install, deploy, execute, or successor recognition
  + direct adapter result equals direct Slice7 core response for the same typed request

& [Acceptance]
  + metadata and argument limits are deterministic closed and honest
  + direct and official rmcp client calls retain exact structured responses for valid and refused requests
  + unknown tool method and extra argument fields fail closed
  + focused debug release workspace lint format evidence and diff gates pass before closure

& [Locked]
  + lifecycle authoring repair custody compression server state HTTP network provider runner signing global consumption admission installation deployment artifact execution internal llama.cpp FPGA remote OneDrive EVO-X2 and Minecraft
