Subject: Semantic Anchor Catalogue P0 Slice 3 Activation Source

@ [activation_source_uuid] 83ea8e7b-01dd-48e8-b4af-e1f0763efbde
@ [conversation_uuid] 019f94c8-da23-7131-aef2-6ac02612d155
@ [captured_at_utc] 2026-08-20T02:10:39Z
@ [user_direction] "set up another 4-hour work shift now. git er done!"
@ [canonical] C:\Project\Cantor\specifications\Cantor_Semantic_Anchor_Catalogue_P0.sop
@ [prior_proof] C:\Project\Cantor\proofs\Cantor_Semantic_Anchor_Catalogue_P0_Slice2_Proof.sop

& [PreservedDirection]
  + continue the published Cantor workstream for approximately four hours
  + after publishing Slice2 select the next dependency-safe local milestone
  + keep production moving through source-preserved SJS-governed implementation and Git publication
  + preserve direct Cantor query iterative-attention and Minecraft separation as rollback boundaries

& [SelectedImplementationClaim]
  + activate only Slice3ExactScanner from the canonical graduation plan
  + accept one DerivedSemanticAnchorCatalogue one exactly matching already admitted SemanticFabric one valid AnchorQuery and an optional scanner-issued continuation
  + validate the complete derived catalogue by canonical rebuild against that fabric before gathering any candidate
  + allow only exact_identity exact_label declared_applicability and typed_relation channels
  + gather supplied known identities before normalized exact labels while preserving each original basis
  + apply lifecycle read authority purpose use-case context and include-exclude boundary gates before expanding applicability or relations
  + treat missing declared use-case boundary or structured operation facts as unresolved guards and proof omissions rather than inferred mismatch
  + join declared applicability only from exact admitted ApplicabilityBinding values that pass their status purpose context boundary authority and non-transfer guards
  + expand only explicitly allowed typed relations from already gate-admitted seeds within maximum paths and depth
  + retain AssociationContribution channel-local value evidence guards and status without scalar score conversion
  + order candidates by PriorityLattice then channel-local rank then stable SemanticAddress identity
  + preserve winning-tier ties as explicit ambiguity
  + exact-resolve every candidate against both the derived identity entry and the admitted fabric unit and package
  + bind proof and continuation to exact request catalogue and fabric roots
  + clip candidates paths records source anchors and serialized output honestly under declared budgets

& [ScannerResult]
  + use the closed AnchorQueryResult candidate boundary proof source-anchor continuation and digest forms stabilized by Slice1
  + return candidate-level exact proof and record identities while leaving requested compact semantic detail projection and direct-query equivalence to Slice4
  + list every unsupported later channel and unavailable structured gate as an omission
  + keep unknown input terms separate from address-bearing candidates
  + a root-bound deterministic continuation may resume only the same request against byte-identical catalogue and fabric roots

& [Acceptance]
  + exact known identity outranks exact label and lower-tier magnitude cannot alter that ordering
  + shared exact labels retain all winning candidates as ambiguity without arbitrary collapse
  + purpose lifecycle boundary authority and absent-declaration gates produce visible eligible excluded stale unauthorized or unresolved disposition
  + unknown known identities and unmatched terms remain accounted without invented addresses
  + allowed typed relation traversal preserves ordered relation identities endpoints direction and hop depth
  + disallowed relation types excessive depth cycles and path-budget overflow do not leak into retained paths
  + unsupported lexical embedding learned provider or attention channels fail before scanning
  + stale catalogue root mismatched fabric altered continuation and continuation exhaustion fail closed
  + candidate record path byte and continuation budgets produce deterministic clipping and a root-bound next cursor when permitted
  + equal inputs and continuation positions produce byte-identical results and digests
  + focused adversarial tests workspace regression warnings-denied clippy format and recursive evidence gates pass

& [Boundary]
  + no lexical token scoring embedding vector model learned routing provider call prompt construction attention bridge persistence network remote host OneDrive hidden state effect FPGA or Minecraft behavior
  + no package compilation signing trust-store mutation source admission revocation polling or live-clock ranking
  + no authority capability operation use-case context boundary or semantic meaning is inferred from natural-language resemblance
  + elapsed-millisecond budget is validated and proof-recorded but deterministic core scanning uses count and byte budgets without consulting a clock
  + direct Cantor query and iterative-attention behavior remain unchanged independent paths

& [Rollback]
  + remove the Slice3 scanner forms functions tests activation review phase lock solution proof and records
  + preserve published Slice1 forms Slice2 derivation existing direct query and every iterative-attention release path without migration
