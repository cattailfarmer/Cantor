Subject: Cantor Compact Procedure Reflection Loop P0 Solution

@ [solution_uuid] 4b9000ac-9b6e-49b5-8636-2c9192c8b329
@ [canonical] C:\Project\Cantor\specifications\Cantor_Compact_Procedure_Reflection_Loop_P0.sop

& [Implementation]
  + cantor_compact_reflection_loop pure library owns request call session advancement reflection and final admission contracts
  + cantor-compact-reflection-loop CLI admits bounded context prompt quota timeout session output and loopback provider inputs
  + host opens exact context before inference and never exposes registry sequence record or checkpoint values as model tool arguments
  + one validated quota call advances the immutable compare-and-set handle and requires terminal state
  + exact READ observation becomes the separate reflection-pass tool message
  + final JSON is admitted only when session outcome status and fixed authority statement match exactly

& [Safety]
  + remote or authenticated provider URLs fail configuration
  + context and response sizes quota prompt timeout output and model count are bounded
  + output is create-new and provider-private reasoning fields are recursively removed
  + the executable invokes no effect and contains no remote fallback OneDrive access or automatic EVO-X2 contact

& [ProofShape]
  + four protocol tests cover exact direct-core equality complete model-tool-model identity and adversarial response and trace boundaries
  + three process tests cover help missing inputs remote rejection empty context and create-new preservation
  + full workspace regression and warnings-denied Clippy pass through the bounded reusable verification lane

& [LocalProofUsability]
  + optional model identity must exactly match the advertised set when the provider exposes more than one model
  + provider-neutral root health replaces a llama.cpp-specific health-path assumption
  + fixture-context emits a create-new deterministic CoordinationToolContext whose identities declare experimental local proof
  + one real loopback HTTP process test drives the executable through health discovery two chat requests compact terminal execution sanitization and report persistence

& [ReportReplay]
  + RunReport is a strict serializable and deserializable public form
  + verify_report reconstructs the deterministic first request and exact first tool call
  + it parses the terminal record reconstructs the fresh single-session registry validates it and compares an existing INSPECT handle
  + it rebuilds the reflection request admits the final response rejects private fields and requires exact nonclaims
  + inspect_report returns a compact identity projection only after complete verification
  + CLI verify and inspect read no historical context file and invoke no provider or procedure

& [Deferred]
  + live generative-provider acceptance multi-call pause continuation compressed terminal projection persistent custody authentication and external effects
