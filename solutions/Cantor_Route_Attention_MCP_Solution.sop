Subject: Cantor Route Attention MCP Solution

@ [solution_uuid] 935d2f86-84fa-48b0-96cf-29095d71a262
@ [canonical] C:\Project\Cantor\specifications\Cantor_Route_Attention_MCP.sop
@ [evidence] C:\Project\Cantor\experiments\needle2_attention_mcp\evox2_acceptance_2026-08-18.json

& [Implementation] is a separate safe-Rust STDIO MCP server
  + publishes exactly route_attention with read-only non-destructive non-idempotent closed-world annotations
  + loads a closed absolute-path config and preflights exact controller runtime config deployment catalogue and health identities
  + rehashes mutable dependency paths before every command to close post-start replacement

& [ProcessBoundary] is direct bounded route-only execution
  + passes one bounded stimulus without a shell
  + uses exclusive temporary stdout and stderr captures with stdin null output polling timeout kill and cleanup
  + invokes the exact runtime verifier after selection and returns success only for account-verified route evidence

& [SemanticBoundary] is learned proposal not signed authority
  + query_sop remains the distinct deterministic signed semantic tool
  + adapter calls neither query_sop nor llama.cpp and authorizes no effect

& [MeasuredResult] is passed
  + four focused tests official MCP client clippy and release build pass
  + EVO-X2 selects and verifies What is Cantor while refusing ungrounded Weaver and ambiguous expanded phrasing
  + full workspace tests and deny-warning all-target all-feature clippy pass after governed current-evidence metadata reconciliation
