Subject: Cantor Provider-Free Private Beta Workflow P0 Solution

@ [solution_uuid] 17123f9a-19ad-46be-8712-4a849de6637d

& [Implementation]
  + strict Windows producer binds published HEAD Cargo.lock and four exact local release binary identities
  + one validated absent non-reparse disposable root contains installed copies ephemeral fixture keys compiled tracked corpus loopback configuration supervisor state logs and generated requests
  + existing Windows PowerShell supervisor scripts provide authenticated readiness fresh health exact-process stop and state removal
  + one generated query-cantor service result is compared byte-semantically with the stopped-service direct CLI fallback ProtocolResponse
  + atomic sanitized report publication occurs only after the complete disposable root is absent

& [IndependentVerification]
  + strict verifier closes every report nested field set and rechecks source commit Cargo.lock current release binary identities corpus manifest counts step order lifecycle equality rollback and capability denials
  + adversarial script executes one real workflow then refuses three unsafe producer inputs and nine report tamper classes

& [Boundary]
  + fresh signing seeds are fixture mechanics destroyed before reporting and never production trust evidence
  + no provider remote persistent effect FPGA Minecraft OS-installer upgrade or operator-product capability
