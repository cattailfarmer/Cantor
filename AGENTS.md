# Cantor Project Instructions

This directory is the official project root for Cantor, the SOP semantic coprocessor.

The canonical remote is `https://github.com/cattailfarmer/Cantor`.

The live reasoning authority is inherited from `C:\Project\ReasoningFramework\AGENTS.md`.

All dictated build specifications must be preserved under `source_documents/` before SJS processing. Canonical specifications belong under `specifications/` and require the satisfaction-signature protocol UUID `ad10f10f-d506-48ef-a805-f8b0a133766c`.

Every conversation turn that changes project understanding, intention, specification, planning, implementation, proof, or unresolved state must append a durable `.sop` turn record under `narrative/turns/`. Turn records must cite their conversation UUID, source anchors, affected subjects, intention, result, and unresolved frontier.

Do not treat nearby Pinky projects, including `slm_project`, `AttentionFramework`, or Holographic Universe, as part of this project unless a later signed specification imports them explicitly.

Historical artifacts may contain absolute paths rooted at `C:\Project\Pinky\LargeLanguageModelWalker`. Preserve those paths as provenance. New governed work must use `C:\Project\Cantor`.
