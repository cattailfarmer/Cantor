Subject: Cantor EVO-X2 Scratch Build Executor P0 Threat Review

@ [threat_review_uuid] 01940b09-cf68-4271-9dec-67f69e41b573
@ [canonical_uuid] 935e020f-8c6f-49e4-b355-63eabd3b778b

& [Threats]
  + unsigned changed duplicate reordered or noncanonical commission promotes plan data into authority
  + package plan request source commit archive digest command set host root or operation coordinate substitution runs different work
  + archive traversal absolute path case collision symlink hardlink reparse point or file-count expansion escapes custody
  + pre-existing root protected overlap disk exhaustion target expansion output flood timeout nonzero exit or process residue corrupts bounded execution
  + missing toolchain or dependency provokes install online resolution external fallback or weakened gates
  + receipt truncation record deletion digest tamper false physical-build flag or conservation drift promotes a failed run

& [Controls]
  + strict canonical reserialization fixed-domain digests satisfaction signature exact package manifest absent-root preflight and closed dispatch
  + bounded member scan link refusal staged extraction atomic establishment exact argv and environment process timeout output hashing stop-on-first-failure and retained forensic state
  + independent dual-PowerShell receipt replay protected/provider before-after equality and persistent process zero

& [ResidualRisk]
  + host toolchain cache and filesystem performance may change after observation and one run cannot characterize future builds
  + successful retained scratch roots require a separate cleanup decision and do not authorize recurrence
