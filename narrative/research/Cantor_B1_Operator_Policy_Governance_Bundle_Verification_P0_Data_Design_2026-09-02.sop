Subject: Cantor B1 Operator Policy Governance Bundle Verification P0 Data Design

@ [data_design_uuid] e8841eed-c313-4fba-8a66-609867fc8a1d

& [Forms]
  + PolicyPayload binds profile policy and revision UUIDs issuer principal and role subject branch remote domain sequence lineage governed action two decision classes attempts retries cleanup required downstream authorities denials fixture flag production-authority false and payload digest
  + PolicyEnvelope binds profile exact payload Ed25519 public key detached signature signing context and envelope digest
  + VerificationRequest binds source canonical signature predecessor evidence exact A1 descriptor raw envelope byte identity origin class references and request digest
  + VerificationReceipt binds every predecessor payload envelope descriptor and correspondence digest positive correspondence fields all-false authority fields zero effects and receipt digest

& [Relations]
  + descriptor bytes and SHA256 equal raw envelope before parse; envelope payload digest and signature verify; request fixture class equals descriptor origin; receipt fields derive rather than copy
  + required downstream authorities are exactly key_custody revocation_truth current_time live_decision fresh_observation private_execution_permit broker_projection physical_preparation

& [Evidence]
  + exact seven-file manifest binds direct-child name bytes SHA256 and total bytes; independent replay regenerates receipt and manifest accounts
