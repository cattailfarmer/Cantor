Subject: Cantor Self Work Update Broker B1 CDrive Linked Worktree Preparation P0 Revision 0.4 Data Design

@ [data_design_uuid] bce904ed-7881-401f-92bf-42775075e640
@ [source_snapshot_uuid] 17ed9314-3bf0-49e1-9222-3cb1276b6fc9

& [Profiles]
  + request plan local-gate filesystem-observation outcome simulation-receipt and simulation-evidence increment to revision 0.4
  + historical publication-proof remains revision 0.3 and Git observation remains revision 0.2 because their data meanings do not change

& [CorrectiveRequest]
  + adds exact hook_quarantine_root and attribute_quarantine_file strict descendants of the reserved scratch root
  + retains source signature invalidation carrier implementation bookend expected-current proof physical false recovery owner principal common-dir executable bounds and capacity fields

& [LocalGate]
  + adds exact absent quarantine coordinates absent Git-dir info attributes and carrier materialization facts
  + carrier facts bind 4416 mode-100644 entries zero other modes sole attributes blob raw bytes SHA256 zero filter assignments and absent .gitmodules
  + all prior current-local lineage owner volume executable capacity process network and zero-contact facts remain exact

& [PlanAndEnvironment]
  + every one of twelve child argument vectors begins with the same six ordered -c key-value pairs before its preserved operation arguments
  + exact environment grows from eleven to fourteen names by adding GIT_ATTR_NOSYSTEM GIT_NO_LAZY_FETCH and GIT_OPTIONAL_LOCKS
  + plan adds one broker-created empty regular file to five broker-created directories while retaining one Git-created candidate branch metadata entry and 4416-file checkout

& [ProjectionAndFilesystem]
  + projection binds quarantine coordinates carrier mode attributes and submodule facts
  + filesystem observation binds empty non-reparse hook directory zero-byte non-reparse attribute file SHA256 E3B0C44298FC1C149AFBF4C8996FB92427AE41E4649B934CA495991B7852B855 plus exact creation counts
  + consequences separately count planned and actual regular-file creation without changing retained-state semantics

& [Determinism]
  + revised forms remain strict duplicate-free canonical JSON with bounded fields artifacts bytes streams and total deadline
  + each child retains raw ordered argv and the verifier compares every element without normalization
  + provider-only receipt binds corrected request proof plan observations projection consequences and domain-separated self-digest
