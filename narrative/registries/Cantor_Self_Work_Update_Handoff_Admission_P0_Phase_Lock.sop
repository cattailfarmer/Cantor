Subject: Cantor Self Work Update Handoff Admission P0 Phase Lock

@ [phase_lock_uuid] 89c11bb7-b242-4403-84eb-8e83f79c73c3
@ [canonical_uuid] aeed3f2e-1169-43d6-8618-b2f7a8c48bad
@ [signature_uuid] c7fec171-73a4-40b8-97c9-ce6257ce949e

& [Lock]
  + one pure child module beneath cantor_ecosystem workspace_admission one public export one focused test target
  + one narrow refactor extracts the existing pure claim-prefix and receipt-integrity logic while physical validate_request retains identical ordered behavior
  + reuse complete SWA-03 validation exact Phase3A forms and current Phase3 machine forms/0.2
  + no Cargo dependency CLI MCP App Server process filesystem provider publication persistence review cleanup or effect adapter

& [OutputBoundary]
  + request proposal and faults only with prepared_awaiting_physical_revalidation representation_only and quarantine_without_cleanup
  + no current freshness authenticated admission patch bytes applied result verification acceptance or rollback receipt

& [RealizedSurface]
  + crates/cantor_ecosystem/src/workspace_admission/update_handoff.rs is the sole pure child module and workspace_admission.rs has the sole public export
  + validation.rs extracts validate_request_claims while the existing physical validate_request calls that exact prefix before its unchanged file directory and overlap sequence
  + supplied request and receipt integrity reuses CandidateWorkspaceRequest AdmissionReceipt ReceiptBody and the existing sha256_digest encoding
  + crates/cantor_ecosystem/tests/self_work_update_handoff.rs is the sole focused target and covers deterministic success strict refusal bounds and explicit nonfreshness
  + three independent evidence-manifest scripts bind artifact bytes and refuse hash mutation or required-artifact removal
  + Cargo dependencies process routes physical writer and every later-stage authority remain unchanged or absent

& [Invalidation]
  + predecessor bypass Phase3A semantic or fault-order drift receipt self-consistency promoted to authentication historical 0.1 path widening physical call mutation or contradictory evidence

& [Rollback]
  + remove only the handoff child module export tests governance and evidence while leaving the pure helper extraction callable only by unchanged Phase3A validation if retained
