Subject: M2B Supplied Digest Reconciliation Preclosure Evidence Fault

& [FaultIdentity] is exact
  + [fault_uuid] is b8b189be-a316-4495-884b-8f4fe2d34908
  + [stage] is first_full_workspace_debug_after_implementation

& [Observation] is one designed recursive evidence rejection
  + 176 ecosystem unit tests, including all then-current reconciliation tests, passed
  + candidate_workspace_admission_evidence then rejected the changed lib.rs byte count
  + prior manifest recorded 2093 bytes while the new module export produced 2168 bytes

& [Classification] is stale derived evidence, not production behavior failure
  + the changed shared export is intentionally inside multiple historical artifact sets
  + the guard correctly prevented a global-pass claim before metadata-only dependency rehash

& [Containment] is fail closed
  + no failing provisional run is cited as final evidence
  + physical candidate-admission builder and every other physical fixture remain unexecuted
  + current manifests are rehashed dependency-first only after all listed closure artifacts stabilize

& [MetadataToolingFaults] are contained before evidence citation
  + direct script invocation met the host PowerShell execution policy; the identical tracked builder then ran under a process-local ExecutionPolicy Bypass
  + the first reusable rehasher used a newer GetRelativePath API unavailable to Windows PowerShell and stopped before mutation
  + the second discovery scope encountered copied manifests under .local and then the historical Phase6 report whose schema has no artifacts collection
  + discovery was narrowed to tracked crates and experiments evidence locations, frozen manifests were excluded explicitly, and only manifests with artifact collections entered the current graph
  + final update and independent verify-only passes each report 23 current manifests, 1014 artifact references, and zero stale records

& [Prevention] requires recursive manifest validation after every shared file or narrative index change
