Subject: Cantor Local Product Acceptance Windows Application Control Operational Fault

@ [fault_uuid] 921ad901-35ad-4a2c-b5cc-67d7c4985cb7

& [Recognition]
  + native focused lifecycle execution passed reached MCP assertions and then Windows refused a newly linked cantor_lifecycle_tool_loop test executable with operating-system error 4551 before that executable ran
  + after a package-scoped regenerable cleanup removed 3.1 GiB and additional targets ran, Windows refused a newly emitted lifecycle evidence-verifier executable before product code ran

& [Containment]
  + no Application Control policy setting exception signing authority remote host dependency source or product assertion was changed
  + the existing lifecycle proof script gained an explicit bounded local WSL mode using locked offline Cargo one stable local target and a fresh temporary python-to-installed-python3 shim
  + the first WSL probe exposed reliance on default target/debug child paths; the invocation now binds both exact MCP binary paths under CARGO_TARGET_DIR

& [Resolution]
  + the corrected WSL mode passes lifecycle and experiment tests independent provider and provider-unavailable recomputation warnings-denied Clippy and format
  + native Application Control admission remains environmental and is not claimed permanently resolved

& [ClosureCommandFault]
  + one closure command guessed a nonexistent scripts/verify_self_hosted_corpus_evidence.ps1 path after all exact workspace assertions had already passed
  + script inventory showed only the governed build and summarize surfaces; no source or evidence artifact changed and the actual acceptance plus recursive evidence verifiers remained passing
