Subject: Cantor Attention Manifest Refresh P6 Requirement Matrix

& [RequirementMatrix]
  + AMR-01 maps to AccountabilityManifestEntry carrying exact handle type deterministic first-label-or-at-handle display version and record digest without object body collections
  + AMR-02 maps to project_accountability_manifest deriving every BTreeMap member once in canonical handle order with count ledger generation ledger digest and self-excluding manifest digest
  + AMR-03 maps to compile_accountability_manifest_window measuring the complete rendering and refusing zero or one-byte-low budgets without clipping
  + AMR-04 maps to AccountabilityManifestWindow retaining frame identity digest purpose ledger binding complete rendered manifest and window digest
  + AMR-05 maps to materialize_accountable_objects returning canonical requested_handles and corresponding complete current records under MAX_MATERIALIZED_HANDLES 32
  + AMR-06 maps to nonempty count duplicate unknown stale ordering identity body and digest refusal gates
  + AMR-07 maps to ManifestAttentionReceiptSeed binding receipt identity exact window ledger manifest and materialization digests
  + AMR-08 maps to exact BTreeMap coverage equality nonempty rationale and member key-handle correspondence
  + AMR-09 maps to Relevant requiring exact materialization while evidence_refs pass through without verification classification
  + AMR-10 maps to deterministic Held on any Unresolved member and Complete otherwise
  + AMR-11 maps to current-ledger manifest equality and expected window digest checks that stale every prior projection materialization and receipt after generation change
  + AMR-12 maps to ProjectManifest Materialize and AcknowledgeAttention host operations and the existing typed MCP transaction returning successor None and producing no snapshot
  + AMR-13 maps to unchanged predecessor full-register resolution inspection historical read patch admission journal and persistence forms plus their passing fixtures
  + AMR-14 maps to four focused P6 core fixtures seven predecessor journal fixtures seven P0 workspace fixtures six Judgment fixtures fourteen adapter fixtures strict clippy and pending detached clean replay
  + AMR-15 maps to module documentation MCP nonclaims proof boundaries and no model network effect authentication publication or hidden-state access

& [ImplementationState] is all_AMR_01_through_AMR_15_satisfied_locally_pending_detached_clean_replay
