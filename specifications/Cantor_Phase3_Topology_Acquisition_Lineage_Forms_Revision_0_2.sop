Subject: Cantor Phase3 Topology Acquisition Lineage Forms Revision 0.2

Description: Canonical unsigned corrective profile for strict caller-decodable acquisition metadata, separately supplied complete current output-only digest carriers, exact role and scope joining, current rederivation and reconciliation, and output-only ClaimOnly repeated-inventory equality. It supersedes the invalidated 0.1 implementation generation without editing its historical bytes. It grants no implementation, execution, physical acquisition, producer, issuer, consumer, provider, persistence, remote, FPGA, Minecraft, neural, or effect authority until its own exact satisfaction signature exists.

@ [corrective_source] C:\Project\Cantor\source_documents\2026-08-23_cantor_phase3_topology_acquisition_lineage_serde_trait_correction\Cantor_Phase3_Topology_Acquisition_Lineage_Serde_Trait_Correction_Source.sop
@ [corrective_audit] C:\Project\Cantor\narrative\research\Cantor_Phase3_Topology_Acquisition_Lineage_Serde_Trait_Correction_SJS_Input_Audit_2026-08-23.sop
@ [exploded] C:\Project\Cantor\specifications\exploded\Cantor_Phase3_Topology_Acquisition_Lineage_Forms_Revision_0_2.exploded.sop
@ [data_design] C:\Project\Cantor\narrative\research\Cantor_Phase3_Topology_Acquisition_Lineage_Forms_Revision_0_2_Data_Design_2026-08-23.sop
@ [trait_proof] C:\Project\Cantor\narrative\research\Cantor_Phase3_Topology_Acquisition_Lineage_Forms_Revision_0_2_Trait_Feasibility_Proof_2026-08-23.sop
@ [matrix] C:\Project\Cantor\feature_support\Phase3TopologyAcquisitionLineageFormsRevision02_Requirement_Matrix.sop
@ [slice] C:\Project\Cantor\feature_support\slices\Phase3TopologyAcquisitionLineageFormsRevision02.sop

& [CanonicalIdentity] is exact
  + [canonical_uuid] is 92f0d23d-dfb0-42d3-b354-de5b7f48426a
  + [source_snapshot_uuid] is 2e6aaaca-8265-43b4-b0d7-b793013c74fd
  + [SJS_protocol_uuid] is aad1eb00-c43a-40a9-baca-dc75fbe25a6d
  + [signature_protocol_uuid] is ad10f10f-d506-48ef-a805-f8b0a133766c
  + [profile] is cantor-phase3-topology-acquisition-lineage-forms/0.2
  + [requirements] are P3TALF-001 through P3TALF-014
  + [state] is corrective_canonical_unsigned_unlocked

& [HistoricalGeneration] is immutable and nonauthorizing
  + original profile 0.1 canonical UUID 7d87c6e5-2d29-4261-875a-8ceaf10ed9ee remains historical
  + original signature bdfc46ad-1d19-4a11-9051-d770cd96c9aa remains invalidated by 6514d200-20c2-47d8-a927-51d9bdc8ceb0
  + operational fault 657fc4df-e100-4948-8042-7f4714eddc22 remains the exact reason
  + no 0.1 lock proof review or signature may authorize 0.2 code

& [Purpose] is a pure feasible representation waist
  + decode and validate strict metadata without carrier construction
  + bind exact First and Second metadata roles to two separately supplied complete current carriers
  + validate complete metadata common scope and every carrier-exposed join
  + independently rederive both carriers and run exact current reconciliation
  + release one output-only ClaimOnly result only for current Equal
  + preserve physical acquisition admission issuance receipt consumption and truth as absent later stages

& [AuthorityDelineation] is closed
  + MetadataClaim is caller-authored strict transport
  + CarrierArgument is an owned complete current supplied output value
  + Binding is pure validated association and output-only
  + ClaimOnlyResult is pure output retaining complete ancestry
  + ProductionAdmittedLineage IssuedEvidence IssuedReceipt ConsumptionDecision and PhysicalTruth are absent
  - never infer authority from Deserialize private fields owned carriers validation rederivation Equal or serialization

& [AcquisitionIdentity] is exact metadata
  + representation is one private nonzero u64
  + derives Clone Debug PartialEq Eq Serialize Deserialize
  + exposes immutable value getter only
  + no generator randomness clock registry or global state
  - no uniqueness collision freedom occurrence producer identity time order proof or capability claim

& [AcquisitionScopeClaim] is one complete strict metadata product
  + candidate_root String bounded to 32768 UTF-8 bytes without NUL
  + root_identity current StrongFileIdentity
  + root_volume_guid_path validated by the current canonical volume-GUID grammar
  + scanner_profile exact cantor-windows-candidate-topology/0.1
  + machine_forms_profile exact cantor-phase3-machine-forms/0.2
  + topology_forms_profile exact cantor-phase3-topology-forms/0.3
  + topology_receipt_profile exact cantor-phase3-topology-receipt/0.3
  + assembly_profile exact cantor-windows-supplied-topology-inventory-assembly/0.1
  + digest_profile exact cantor-windows-supplied-ordered-topology-inventory-digest/0.1
  + reconciliation_profile exact cantor-windows-supplied-ordered-topology-inventory-digest-reconciliation/0.1
  + encoding_profile exact cantor-ordered-topology-observation-encoding/0.1
  + limits one complete current TopologyScanLimits
  + policy_sha256 exact lowercase 64-hex digest
  + predecessor_admission_profile exact cantor-candidate-workspace-admission/0.1
  + predecessor_receipt_sha256 exact lowercase 64-hex digest
  + derives strict Serialize Deserialize with deny_unknown_fields and private fields
  - no option wildcard default alias partial comparator or separate scope identity

& [CarrierJoinClassification] is exact
  + carrier-exposed encoding_profile must equal metadata encoding_profile and the current constant
  + carrier assembly plan limits must equal the complete metadata limits
  + first ordered observation relative_path must be absent
  + first ordered observation kind must be RootDirectory
  + first ordered observation identity volume_serial and file_id_hex must equal metadata root_identity
  + carrier assembly profile must equal metadata assembly_profile and the current constant
  + carrier digest profile must equal metadata digest_profile and the current constant
  + reconciliation plan and output profiles must equal metadata reconciliation_profile and the current constant
  + candidate_root root_volume_guid_path scanner machine topology receipt policy predecessor admission and predecessor receipt remain exact metadata-only coordinates
  - metadata-only coordinates are never called carrier-derived observed admitted issued fresh or physically proven

& [CompletionDisposition] is closed metadata
  + exactly CompleteClaimed
  + derives strict closed Serialize Deserialize
  - not observed actual proven trusted admitted issued successful or fault-free

& [AcquisitionLineageMetadataClaim] owns no carrier
  + one AcquisitionIdentity one AcquisitionScopeClaim and CompleteClaimed
  + derives strict Serialize Deserialize with deny_unknown_fields and private fields
  + exposes immutable getters
  - not a complete physical lineage binding

& [CausalOrderClaim] is explicit strict metadata
  + CausalOrderKind has exactly FirstCompletionStrictlyPrecedesSecondStart
  + claim owns exact first and second AcquisitionIdentity endpoints and kind
  + endpoints differ and match named pair roles exactly
  + derives strict Serialize Deserialize with deny_unknown_fields and private fields
  - no clock timestamp duration deadline freshness occurrence witness or order inference

& [OrderedAcquisitionPairMetadataClaim] has exact cardinality
  + owns one first AcquisitionLineageMetadataClaim one second claim and one CausalOrderClaim
  + role identities differ
  + complete scope products compare exactly equal
  + causal endpoints and direction match roles
  + derives strict Serialize Deserialize with deny_unknown_fields and private fields
  - no vector optional role duplicate reuse operand-order or JSON-order meaning

& [RepeatedInventoryEvidencePlan] is strict metadata input
  + exact profile cantor-phase3-topology-acquisition-lineage-forms/0.2
  + one complete OrderedAcquisitionPairMetadataClaim
  + one current WindowsSuppliedOrderedTopologyInventoryDigestReconciliationPlan
  + requested_evidence exactly NonAtomicRepeatedInventoryEqual
  + derives strict Serialize Deserialize with deny_unknown_fields and private fields
  + encoded decode limit is 131072 bytes before serde
  - OsSnapshotProven aliases defaults unknown fields compatibility and carrier data are rejected

& [AcquisitionLineageBinding] is output-only
  + owns one validated AcquisitionLineageMetadataClaim and one complete current WindowsSuppliedOrderedTopologyInventoryDigest
  + derives Clone Debug PartialEq Eq Serialize only
  + private fields and immutable getters only
  - no Deserialize DeserializeOwned Default public constructor From Into mutable getter locator or receipt conversion

& [OrderedAcquisitionPairBinding] is output-only
  + owns exact First and Second AcquisitionLineageBinding values and validated CausalOrderClaim
  + derives Clone Debug PartialEq Eq Serialize only
  + private fields and immutable getters only
  - no Deserialize DeserializeOwned Default public constructor conversion or role permutation

& [RepeatedInventoryEvidenceClaim] is output-only
  + owns exact profile complete OrderedAcquisitionPairBinding current complete reconciliation requested NonAtomicRepeatedInventoryEqual and provenance ClaimOnly
  + AcquisitionEvidenceProvenanceGrade has exactly ClaimOnly and derives Serialize only
  + result derives Clone Debug PartialEq Eq Serialize only
  + private fields and immutable getters only
  - no Deserialize DeserializeOwned Default public constructor From Into TopologyReceipt conversion admission issuance or consumption

& [PureConstructionPath] is exact
  + decode_repeated_inventory_evidence_plan accepts bounded bytes and returns strict validated metadata or one fault
  + derive_repeated_inventory_evidence_claim consumes one validated plan first carrier and second carrier
  + first metadata binds only first argument and second metadata binds only second argument
  + function validates in V01 through V12 order and releases one result or one atomic fault
  + no other production function constructs a binding pair or evidence result

& [ValidationOrder] is deterministic
  + V01 reject bytes beyond 131072 before metadata decode then reject serde errors
  + V02 validate exact outer requested and all nested profiles
  + V03 validate first then second identities completion and complete scope fields
  + V04 require unequal identities exact complete metadata scope equality and exact causal endpoints
  + V05 validate exact current reconciliation plan profile and nonzero identity
  + V06 validate first then second carrier digest encoding assembly fixed root shape root identity limits and profile joins
  + V07 independently derive first then second current digest from clones of each retained current plan and assembly
  + V08 require each rederived carrier equals its supplied complete carrier
  + V09 run current reconciliation over the two rederived complete carriers
  + V10 require reconciliation common scope equals every carrier-exposed metadata coordinate and exact current profile
  + V11 map Different separately and require Equal plus requested NonAtomicRepeatedInventoryEqual
  + V12 construct pair bindings and output atomically after every prior gate

& [CurrentRederivation] is mandatory
  + use derive_windows_supplied_ordered_topology_inventory_digest with the carrier plan and assembly
  + execute independently for First then Second
  + compare complete rederived and supplied carriers
  + use reconcile_windows_supplied_ordered_topology_inventory_digests with the exact plan and rederived carriers
  + retain the complete current reconciliation in output
  - never trust digest text or replace current functions

& [FaultModel] is closed and bounded
  + codes are InvalidRequest Profile Identity Scope Lineage Order Reconciliation Different Evidence Resource
  + one fault contains code bounded field at most 64 characters bounded message at most 256 characters and optional boxed current digest or reconciliation fault
  + first governed failure returns and no binding reconciliation or result leaks
  + Different remains valid non-equality and separate from malformed reconciliation
  + impossible carrier Deserialize is a compile-time API denial not a runtime fault

& [SerdeAndTraitLock] is executable
  + AcquisitionIdentity AcquisitionScopeClaim CompletionDisposition AcquisitionLineageMetadataClaim CausalOrderKind CausalOrderClaim OrderedAcquisitionPairMetadataClaim and RepeatedInventoryEvidencePlan implement Serialize plus DeserializeOwned
  + WindowsSuppliedOrderedTopologyInventoryDigest AcquisitionLineageBinding OrderedAcquisitionPairBinding WindowsSuppliedOrderedTopologyInventoryDigestReconciliation and RepeatedInventoryEvidenceClaim do not implement DeserializeOwned
  + compile-time positive generic bounds and negative stable coherence assertions are mandatory under cfg(test)
  + no new assertion dependency is permitted

& [StrictGrammarAndBounds] are exact
  + every metadata struct uses deny_unknown_fields
  + no aliases defaults flatten untagged catch-all maps arbitrary JSON free diagnostics or compatibility fallbacks
  + every profile is exact and at most 128 bytes before comparison
  + candidate and volume paths are at most 32768 UTF-8 bytes without NUL
  + digests are exact lowercase 64 hexadecimal characters
  + fixed two-carrier cardinality and current nested limits bound processing

& [PublicSurface] is bounded
  + one module windows_topology_acquisition_lineage
  + one pub mod export in cantor_ecosystem lib.rs
  + public profile and plan-byte constants types immutable getters decode function and derive function
  + no re-export glob Cargo dependency or current-module modification required

& [EffectBoundary] is empty
  + safe Rust pure CPU and bounded owned memory only after signature
  + no cfg(windows) unsafe platform filesystem handle pointer clock random process environment network persistence database Git model tool fixture execution acquisition admission issuance consumption mutation seal cleanup promotion neural distributed FPGA or Minecraft operation

& [TestAndProofContract] is exact
  + positive metadata round trips unknown-field and encoded-size refusal
  + zero duplicate wrong endpoint scope coordinate profile requested-grade and resource refusal
  + carrier role swap exposed-coordinate mismatch invalid ancestry and rederivation refusal
  + reconciliation structural failure Different and Equal-to-ClaimOnly success
  + complete output ancestry getters serialization and absent Deserialize Default constructors conversions and effects
  + focused debug release full workspace quality evidence provenance rollback attribution and post-push gates

& [InheritedNonclaims] are absolute
  + no acquisition occurrence uniqueness collision freedom physical origin producer identity causal witness time quiescence atomicity snapshot freshness immutability current truth admission issuance receipt conversion consumption or external effect
  + provider-unavailable zero-synthetic-trial evidence is unaffected

& [CandidateImplementation] is bounded but unauthorized
  + at most one new safe Rust module and one lib export
  + zero Cargo lockfile dependency unsafe current-carrier-module platform and effect delta
  + exact profile 0.2 types functions faults tests and compile-time trait assertions only
  + implementation authority is absent until replacement artifact lock proof review and signature

& [SatisfactionCriteria] are P3TALF-001 through P3TALF-014 with C01 through C30 T01 through T18 seven folds and executable trait closure all passing

& [CanonicalDecision] is stable for independent artifact binding
  + unresolved requirement constraint threat fold data trait or Rust-feasibility question is none
  + source revision required is false
  + canonical authority is absent
  + implementation authority is absent
  + execution authority is absent except governance verification already recorded
  + physical authority is absent
  + next is content-addressed readiness audit and signature-pending artifact phase lock
