Subject: Cantor Coordination Transport Measurement P0 Seven-Fold Explosion

@ [explosion_uuid] fed7608e-bba5-4bc9-b51f-33e529793fe1
@ [source_manifest] C:\Project\Cantor\source_documents\2026-08-19_coordination_transport_measurement\Source_Document_Manifest.sop
@ [predecessor] C:\Project\Cantor\specifications\Cantor_Resumable_Coordination_Tool_P0.sop

& [Fold1Identity] measures serialized exchange not neural attention
  + structured bytes are exact UTF-8 compact JSON bytes produced by serde_json
  + context bytes are the six-field CoordinationToolContext value
  + checkpoint bytes are complete CoordinationCheckpoint values
  + no byte value is described as tokens resident memory latency or model capacity

& [Fold2Fixture] is one exact reproducible program
  + use the checked CPPE two-process candidate with explicit measurement identities and budgets
  + derive admitted context through run_authorship_lane rather than hand-constructing authority
  + record fixture source and terminal outcome digest

& [Fold3StaticMetrics] isolates fixed surfaces
  + measure context BEGIN request BEGIN response genesis checkpoint MCP tool metadata input schema and output schema separately
  + serialize every value through the production public type
  + repeat generation twice and require byte equality

& [Fold4ScheduleMetrics] expose chunking cost
  + schedules use maximum step quotas one two four eight and sixty-four
  + each schedule starts from BEGIN then repeatedly ADVANCE until terminal
  + record call and advance counts total request response and structured bytes maximum request and response bytes and checkpoint size range
  + all schedules must report the exact uninterrupted fixture step count and one byte-equal terminal outcome

& [Fold5ContextAccount] bounds possible compression honestly
  + repeated_context_bytes is exact context JSON bytes multiplied by total calls
  + zero_byte_handle_upper_bound removes every repeated context after the first but charges no registry handle bytes
  + context request and total-transfer shares use integer basis points
  + this is a theoretical ceiling and not a registry performance forecast

& [Fold6Artifact] is deterministic and self-verifying
  + one versioned strict report serializes as pretty JSON with a trailing newline
  + report digest is SHA-256 over the compact report with report_digest cleared
  + checked artifact equals fresh generation byte for byte
  + tests revalidate arithmetic schedules terminal equality and digest

& [Fold7Decision] prevents measurement overreach
  + interpret cost across all quotas rather than optimizing quota one alone
  + a registry remains deferred until custody concurrency restart and authentication are separately specified
  + no latency memory token compression general-program provider or hardware conclusion follows from this fixture

& [SelectedFlow] is admitted fixture -> exact static serialization -> deterministic schedule replay -> arithmetic validation -> immutable artifact -> bounded architecture finding
