Subject: Cantor M2B Supplied Topology Inventory Assembly Exploded Specification

& [ExplodedIdentity] is exact
  + [exploded_uuid] is 16cc9b14-bc7b-412e-bf5f-85c38b96e0a8
  + [source_snapshot_uuid] is 29bd88c2-59c9-48b2-b245-a5e30a39eeac
  + [profile] is cantor-windows-supplied-topology-inventory-assembly/0.1
  + [target_module] is crates/cantor_ecosystem/src/windows_supplied_topology_inventory_assembly.rs

& [Delineation] separates evidence grades
  + carrier validity is output-only pure projection evidence
  + current composition validation is M2A form revalidation only
  + supplied-set closure is exact correlation among provided carriers only
  + physical origin, enumeration, completeness, traversal, digest, receipt, and admission remain absent

& [M2B-SSTIA-001] explodes strict plan
  + exact profile, nonzero assembly identity, complete TopologyScanLimits, 16384-byte predecode cap, denied unknown fields, current limit validation
  - no entry, count, total, digest, receipt, or provenance claim in plan

& [M2B-SSTIA-002] explodes input ownership
  + exactly one root wrapper and owned directory and regular-file wrapper vectors
  + closed Serialize-only member union owns complete carriers
  - no raw observation, lower fragment, deserialized carrier, or generic conversion

& [M2B-SSTIA-003] explodes current validation
  + current ValidateTopologyForm on limits and every projected observation
  + exact nested TopologyFormFault only under topology_form
  + carrier-specific kind gates remain distinct
  - no false claim that lower validations rerun

& [M2B-SSTIA-004] explodes cardinality and root
  + checked usize additions, checked u64 conversion, maximum_entries gate before internal vector growth
  + root must remain RootDirectory, None path, and ordinal one
  + one root is guaranteed by function shape rather than counted from a raw collection

& [M2B-SSTIA-005] explodes carrier kinds
  + directory carrier observation is exactly Directory
  + regular-file carrier observation is exactly RegularFile
  + root carrier observation is exactly RootDirectory

& [M2B-SSTIA-006] explodes correlation identities
  + projection identity is exact from carrier plan and globally unique
  + entry reference is exact from root/directory stable pair or regular-file content-binding stable pair and globally unique
  - neither identity is physical provenance

& [M2B-SSTIA-007] explodes strong identity
  + every full identity validates under M2A
  + every volume_serial equals root volume_serial exactly
  + complete StrongFileIdentity values are globally unique including root
  - no partial identity, normalization, platform lookup, root membership, or physical-volume claim

& [M2B-SSTIA-008] explodes path identity
  + root path is None
  + every descendant path is Some and already current-M2A-valid
  + exact String uniqueness crosses directory and regular-file carriers
  - no case folding, Unicode normalization, filesystem collation, or Git truth

& [M2B-SSTIA-009] explodes structural limits and accounting
  + count, UTF-8 path bytes, slash-component depth, streams per entry, per-file length, and checked total file bytes obey supplied valid limits
  + root and directory contribute zero file bytes
  + deadline retained without clock or freshness inference

& [M2B-SSTIA-010] explodes parent closure
  + root is implicit parent for top-level paths
  + every proper path prefix exists exactly once after duplicate rejection
  + every prefix kind is Directory
  - no sibling, child, ignored-entry, nested-repository, or completeness claim

& [M2B-SSTIA-011] explodes structural order
  + root fixed first
  + descendant components compare exact raw UTF-8 bytes
  + equal shared prefixes place shorter sequence first
  + duplicate paths reject before order and no secondary tiebreaker exists
  - no physical, Windows, Unicode-normalized, case-insensitive, or Git-order claim

& [M2B-SSTIA-012] explodes ordinal binding
  + root ordinal one
  + sorted descendant positions require exact ordinals two through entry_count
  + checked conversion and addition only
  - no generated, repaired, or traversal-proven ordinal

& [M2B-SSTIA-013] explodes output
  + private profile, plan, ordered_members, entry_count, and total_file_bytes
  + Serialize only, immutable getters, no constructor, Default, Deserialize, public fields, mutable getters, From, or Into
  + complete carriers occur exactly once in output member sequence

& [M2B-SSTIA-014] explodes faults
  + closed codes json, profile, assembly_identity, topology_form, cardinality, root_shape, carrier_kind, projection_identity, entry_reference, volume, strong_identity, relative_path, ordinal, limit, parent, order, arithmetic, resource
  + 64-scalar field and 256-scalar message
  + atomic failure and no partial success

& [M2B-SSTIA-015] explodes effects and provenance
  + deterministic in-memory safe Rust with existing serde and types only
  + zero Cargo, lockfile, unsafe, platform-call, filesystem, process, environment, time, random, network, persistence, Git, model, fixture, traversal, digest, receipt, admission, mutation, seal, cleanup, and promotion delta

& [M2B-SSTIA-016] explodes proof closure
  + direct happy, boundary, duplicate, limit, parent, order, ordinal, arithmetic, JSON, and diagnostic tests
  + independent static test and clone-portable evidence rehash
  + focused and workspace debug and overflow-checked release
  + format, warnings-denied lint, release build, warnings-denied docs, audit, recursive evidence, narrative, and rollback
  + zero physical execution

& [DataDesign] is ownership explicit
  + plan owns validated supplied limits
  + member enum owns one complete carrier variant
  + output owns one root-first ordered member vector and derived scalar totals
  + lookup sets and maps are evaluation-local and are never serialized as authority
  + no carrier is cloned merely to create an authority-equivalent second representation

& [EvaluationOrder] is phase locked
  + plan then cardinality then root validation
  + descendant validation and kind gates while extracting exact facts
  + volume and five duplicate classes before limits, parent closure, sort, and ordinal comparison
  + accounting uses checked addition and first failure returns no output

& [ConstraintReapplication] is complete
  + output-only carrier boundary survives every convenience proposal
  + exact comparisons survive normalization proposals
  + parent closure survives completeness overclaim review
  + deadline survives without clock use
  + aggregate digest and receipt remain removed
  + all effect paths remain removed

& [SevenFolds] are phase locked
  + [semantic] supplied structurally closed set only
  + [security] nonforgeable carrier inputs, private output, no downgrade
  + [failure] closed bounded atomic faults
  + [resource] cap before internal growth and checked arithmetic
  + [provenance] current M2A revalidation distinguished from lower and physical evidence
  + [composition] exact carrier ownership and canonical structural sequence
  + [verification] direct, static, global, recursive, and rollback evidence

& [Pruning] removes unsafe growth
  - no raw observation overload
  - no combined JSON input for carriers
  - no digest, receipt, consistency, freshness, or admission field
  - no generalized path comparator beyond this profile
  - no physical or asynchronous seam

& [GrowthSeeds] remain downstream only
  + canonical encoding can consume the stable ordered member sequence later
  + physical producer can produce carriers with separate runtime provenance later
  + double inventory can compare two separately proven physical assemblies later
  + receipt issuance remains gated by physical, digest, policy, and freshness evidence

& [ExhaustionState] is phase_locked_for_pure_implementation
