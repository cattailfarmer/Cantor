Subject: Cantor M2B Supplied Root Topology Projection Plan

& [PlanIdentity] is governance ordered
  + [plan_uuid] is 3762546a-44d3-4b14-8e9b-93c6d0239004
  + [profile] is cantor-windows-supplied-root-topology-projection/0.1
  + [status] is authorized

& [Steps] are exact
  + verify source, manifest, review, and three current prerequisite signatures and hashes
  + complete every SJS stage and issue separate explosion, canonical, justification, plan, registry, matrix, slice, and signature
  + implement one pure module and export with zero dependency, unsafe, platform-call, or effect delta
  + test strict plan, current preflight validation, disposition, current stability, reference, kind, identity, both component extractions, exact joins, mapping, M2A validation, wrapper, faults, and serialization
  + add independent static and clone-portable evidence tests
  + run focused and workspace debug and overflow-checked release, format, warnings-denied lint, release build, warnings-denied docs, audit, and recursive evidence
  + publish solution, proof, review, narrative, reentry, and rollback

& [StopRules] are fail closed
  + stop on any source, SJS, prerequisite, or hash drift
  + stop if either current validator is bypassed
  + stop if a bare success fragment can establish output
  + stop if any join normalizes or maps partial identity
  + stop if fixed root shape becomes caller-selected
  + stop if success is forgeable, deserializable, mutable, or downgradable
  + stop on physical, traversal, inventory, Git, receipt, admission, mutation, sealing, cleanup, or promotion behavior or claim

& [Completion] authorizes only crates/cantor_ecosystem/src/windows_supplied_root_topology_projection.rs, one export, pure tests, evidence, and closure; physical execution is forbidden
