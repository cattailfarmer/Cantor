Subject: Cantor Identity Accounting MCP Durable Store P2 Plan

& [ImplementationSequence]
  + add a dedicated workspace crate with store MCP server and stdio binary
  + implement bounded scan prefix recovery persist-before-publish and strict typed MCP wrapper
  + add deterministic direct restart concurrency malformed store metadata and official subprocess fixtures
  + verify focused crate workspace clippy full relevant suites and clean-checkout replay
