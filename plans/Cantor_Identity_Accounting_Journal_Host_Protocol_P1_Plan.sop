Subject: Cantor Identity Accounting Journal and Host Protocol P1 Plan

& [ImplementationSequence]
  + add one shared_attention journal-host module and public re-export without touching foreign lib.rs or visualizer work
  + implement journal events snapshots replay validation canonical encoding strict protocol requests results responses and transitions
  + reuse P0 projection resolution inspection and compare-and-set patch functions as the only semantic object operations
  + add deterministic genesis replay mutation stale root read-only ambiguity canonical restore tamper and strict-form fixtures
  + run formatting focused tests full cantor_core tests clippy clean-checkout replay and governed bookends
