Subject: Cantor EVO-X2 Scratch Build Executor P0 Package Canonical Copy Correction Proof

@ [proof_uuid] 129a90ff-bef4-4319-a267-5b112bd4ea20
@ [fault_uuid] 2722ea37-9865-4d8a-a92d-f05f8e4ed232
@ [canonical_uuid] 935e020f-8c6f-49e4-b355-63eabd3b778b

& [Correction]
  + package construction retains all implementation and evidence inputs from the exact implementation archive
  + the three predecessor JSON fixtures alone cross an explicit repository-storage to runtime-machine-form boundary that removes exactly one LF or CRLF terminator
  + UTF-8 BOM invalid encoding absent terminator repeated terminator and empty canonical content refuse before package composition
  + the independent Rust package verifier remains unchanged and must parse reserialize recompute predecessor verification hash every member and verify the aggregate before success

& [LocalEvidence]
  + PowerShell7 and WindowsPowerShell5.1 parse the corrected builder and the harness evidence verifier requires the canonical-copy refusal tokens
  + recursive current evidence replay and exact workspace debug release Clippy format and diff gates must pass before correction publication

& [ClaimBoundary]
  + this proof authorizes only publishing and immediately bookending the provider-free local correction before a create-new package retry
  + the retained first root remains failed evidence and must never be transferred or treated as a package
  + provider_requests0 remote_calls0 effects0 live_effects_authorized false physical_build_performed false
