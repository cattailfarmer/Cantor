Subject: Cantor Succeeding SOP Fixture Rollback P0 Upstream Correction Proof

@ [proof_uuid] a960236f-d125-47e8-ac36-b553bfa8f6b9
@ [canonical_uuid] 18cf8fd3-dfad-4b6f-a693-a9657c70cc32

& [CorrectedFixture]
  + predecessor raw bytes are exactly 93 LF bytes
  + predecessor SHA256 is e8405fae0ee54f9fdfb39ea84b3fa6e710c5163fbc916b11a9efefddf0564a61
  + regenerated B2A receipt is 49484 bytes SHA256 018D955E43C13EF8CC294F3271326824443DEB56F8C5FD00730ABB05A1B85E1E with receipt digest 4b62e233f8d61156a9fc0835e34d6d30fbc2588cb2230f5a1bf1750347cfe360
  + current_source_bytes and rollback_source_bytes are typed bounded equal and both exactly 93

& [Implementation]
  + safe B2B2 Rust consumes exact B2B1 receipt observes exact current successor rehashes predecessor and failed candidate advances generation 42 to 43 replaces the strict registry and stops awaiting boot validation
  + failed-candidate observation independently binds physical metadata length and reopened byte length to source_reacquisition.source_bytes before accepting the candidate SHA256 or creating the temporary registry
  + controlled evidence production validates three distinct ordinary JSON outputs beneath the governed artifact root and rejects reparse ancestors before WSL contact and again before writing validates both 93-byte receipt snapshots plus the predecessor raw digest and emits controlled and manifest JSON as deterministic UTF-8 without BOM and LF
  + strict receipt replay mirrors every executor correspondence for the failed registry and source; the Linux-only adversary fully rehashes a substituted source path and receives InvalidRegistry as required
  + the controlled evidence manifest source ledger enumerates the complete SFR-A01 formation implementation completion navigation and attribution graph rather than a partial governance subset
  + manifest File-Ref acquisition is repository-contained and no-follow for both the input leaf and every existing ancestor before hashing
  + manifest assembly refuses duplicate normalized paths and binds the exact acquired file-reference count
  + controlled receipt admission independently binds all required positive physical outcome flags and both exact 93-byte source paths and SHA256 identities before evidence production
  + controlled test totals are bound to one exact WSL gate receipt emitted only after all six test targets warnings-denied Clippy and format succeed
  + a separate provider-free read-only verifier independently rehashes the exact fifty-seven-file set and rechecks sorted unique manifest shape controlled and rollback outcome boundaries for post-push replay
  + native Windows PowerShell 5.1 disposable-temp replay proves verifier success manifest-order laundering refusal unknown-field refusal source-tamper refusal and fully rehashed provider-outcome laundering refusal after the Path.GetRelativePath compatibility fault was removed
  + exact core rollback-byte source-path and deterministic nonphysical-activation tests pass individually on the final tree
  + warnings-denied Clippy and format pass for the bounded core ecosystem module tests and development fixture example; registry_path is test-only so the Linux example introduces no dead-code warning
  + directly affected exact-current native debug passes combined B2B1 plus B2B2 fourteen tests and standalone B2B2 eight tests; B2A passed forty-three before the ecosystem-only audit correction and afterward passed forty-two before one unskipped nested CLI subprocess was refused by Application Control with no semantic assertion failure
  + the preceding schema revision passed overflow-release B2A forty-five combined fourteen and standalone eight; after all audit corrections exact-current combined overflow-release passes fourteen while Application Control error 4551 refuses the fresh B2A and standalone release executables before execution

& [NotYetProved]
  + WSL durable success exact-current overflow-release independent evidence and exact workspace gates are proved; final closure recursive rehash publication remote equality and post-push replay remain
  + no completion or product activation claim is authorized
